{
  "source": "verify-agent",
  "status": "Pending",
  "phase": "5",
  "spec_version": "spec_v1",
  "spec_fingerprint": "sha256:0662c9e198c2f7b5cb4fa50d1b1f0bf9e2cce901454ffbf9c046285a645fc18a",
  "created_date": "2026-08-21",
  "updated_date": "2026-08-21",
  "out_of_spec": false,
  "dependencies": [],
  "decision_dependencies": [],
  "verification_attempts": 0,
  "notes": "[CLAUDE'S HALF DONE 2026-08-21 — investigation complete, decision + action are Erik's]\n\nLive workspace enumerated first-hand (Fabric items API, HTTP 200, 15 items) and BOTH orphans confirmed present today. Both definitions fetched and read.\n\nTHE RECORD WAS WRONG AND IS CORRECTED IN THE DESCRIPTION ABOVE: the premise this task was filed on — that tmp_task064_seed holds rollback DDL that would silently undo the DECIMAL fix — is FALSE against the live definition. Its CREATE TABLE is DECIMAL(18,2) on both columns, matching azure/procurement.sql. Only the comment header still narrates the old SMALLINT/FLOAT rollback. The pipeline is still destructive (DROP + reseed) and still undocumented, which is why the task stands — but not for the reason it was filed.\n\nuser_review_pending set so this surfaces on the dashboard: deleting live workspace items is Erik's call, not Claude's, and the remaining work is a decision plus a Fabric UI action.",
  "id": "task-079",
  "difficulty": 3,
  "owner": "both",
  "spec_section": "Orchestration",
  "files_affected": [
    "docs/architecture/fabric-artifacts-inventory.md"
  ],
  "title": "Reconcile live-workspace items that source control does not describe",
  "description": "Deploy run 32427236326 (2026-08-20T23:07:16Z) enumerated 15 deployed items against 12 publishable repo items. Re-confirmed FIRST-HAND against the LIVE workspace on 2026-08-21 via the Fabric items API (15 items, HTTP 200) — both orphans are still present today, not merely in yesterday's log:\n  Notebook/Notebook_1              id c4749a34-1522-4d38-93ce-52710b75461b\n  DataPipeline/tmp_task064_seed    id d703e278-71e8-4851-9468-4a6268cee743\n(The third workspace-only item, SQLEndpoint/oem_lh, is Fabric-generated by the Lakehouse — benign, no action.)\n\nDEFECT 1 — FALSE RECORD, CONFIRMED. docs/architecture/fabric-artifacts-inventory.md:51 states Notebook_1 was 'Removed 2026-08-10'. It was not. Scoped check of that same 'removed 2026-08-10' block: of its 8 rows, Notebook_1 is the ONLY one falsified — EPI_file2table, WGI_file2table, oem_wh, both Staging* items and the Report Usage Metrics pair are all absent from the live enumeration. So this is one bad row, not a systemic failure of that table.\n  Notebook_1's content was read: three scratch cells (fact_procurement .schema; a gold_quality_history query for dq_data_type_consistency; a table loop). Harmless DQ-investigation scratch, never in the repo.\n\nDEFECT 2 — UNDOCUMENTED LIVE PIPELINE. tmp_task064_seed appears NOWHERE in the inventory doc (0 matches; positive control in the same probe: Notebook_1 matches once, so the probe works).\n\n*** CORRECTION TO THE PREMISE THIS TASK WAS FILED ON — MEASURED, NOT INFERRED ***\nPhase-level verification filed this citing task-069's record, which says tmp_task064_seed 'HELD THE ROLLBACK DDL (Quantity SMALLINT / UnitPriceEUR FLOAT), not the repo's DECIMAL(18,2). Running it unmodified would have silently undone the fix while reporting success.' THAT IS NO LONGER TRUE OF THE LIVE PIPELINE. Its definition was fetched and parsed 2026-08-21: one Script activity, whose CREATE TABLE reads Quantity DECIMAL(18,2) and UnitPriceEUR DECIMAL(18,2) — byte-identical in those two column declarations to the repo contract in azure/procurement.sql. SMALLINT appears once and FLOAT four times in the script, ALL of them inside the stale comment header that still narrates the old rollback.\n  So the hazard is not what the record says. Someone corrected the DDL body and left the 'ROLLBACK DDL' comment header in place. What remains live is: (a) a genuinely DESTRUCTIVE pipeline — it DROPs dbo.procurement_transactional and re-seeds it with one INSERT of the 132 rows — that (b) any reader would mis-read from its own header as a schema revert. It rebuilds the CORRECT schema; it does not revert the fix.\n\nDO: (a) decide whether tmp_task064_seed is still wanted as a rebuild path — the repo already carries the same DDL+seed in azure/procurement.sql + azure/procurement_seed.sql, which is the argument for deleting it; (b) delete both items, or document them in the inventory with purpose and retention; (c) correct inventory line 51 so the 'Removed' column reflects measured workspace state, and if tmp_task064_seed is kept, add it with its comment header fixed. Verify by re-enumerating the live workspace (Fabric items API or the next deploy log's 'Currently deployed items' group), not by inspection.",
  "acceptanceCriteria": [
    "Notebook_1 and tmp_task064_seed are each either deleted from the live workspace or documented in fabric-artifacts-inventory.md with purpose and retention.",
    "fabric-artifacts-inventory.md:51's 'Removed 2026-08-10' claim for Notebook_1 is corrected to reflect measured workspace state.",
    "If tmp_task064_seed is kept, its stale 'ROLLBACK DDL (SMALLINT/FLOAT)' comment header is corrected — the script it heads creates DECIMAL(18,2).",
    "The end state is confirmed by re-enumerating the live workspace, not by inspection alone."
  ],
  "user_review_pending": true,
  "interaction_hint": "dashboard"
}

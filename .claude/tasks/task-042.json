{
  "id": "task-042",
  "title": "Single-source the EPI vintage \u2014 bronze-to-silver still hardcodes bronze_epi2024results while both ends now derive the year",
  "description": "task-028 parameterized the EPI table-name year at two of three sites: bronze_ingest_epi derives bronze_epi{p_epi_year}results and silver-to-gold2 derives silver_epi{EPI_YEAR}results from a module constant. bronze-to-silver.Notebook (lines 74/108) still hardcodes bronze_epi2024results -> silver_epi2024results, and data_quality_checks.Notebook hardcodes the same names in its schema/rowcount contracts. So the year is parameterized at both ends and hardcoded in the middle: a genuine 2025 pull would land in bronze_epi2025results and be silently ignored by silver and by the DQ suite. The residual failure mode is safe (bumping EPI_YEAR fails loudly on a missing silver table) rather than the silent mislabeling task-028 fixed, which is why this is a follow-up rather than a defect in task-028's delivery. NOTE: spec_v1 \u00a7 Data Sources #2 states 'Bronze table: bronze_epi2024results' / 'Silver table: silver_epi2024results' as FIXED names, so closing this properly needs a spec reconciliation through /iterate \u2014 state the naming convention (bronze_epi{year}results) plus the single-vintage caveat, and decide whether bronze-to-silver should be parameterized or whether single-vintage is the intended design.",
  "status": "Finished",
  "priority": "medium",
  "difficulty": 3,
  "owner": "both",
  "phase": 2,
  "phase_name": "Silver/Gold transforms + DQ gate",
  "spec_section": "Data Sources",
  "spec_version": "spec_v1",
  "estimated_hours": 1.5,
  "files_affected": [
    "fabric/bronze-to-silver.Notebook/notebook-content.py",
    "fabric/data_quality_checks.Notebook/notebook-content.py",
    ".claude/spec_v1.md"
  ],
  "acceptanceCriteria": [
    "The EPI vintage has exactly one source of truth across bronze_ingest_epi, bronze-to-silver, silver-to-gold2 and data_quality_checks \u2014 no notebook hardcodes a year literal in an EPI table name.",
    "A wrong-vintage run fails loudly rather than silently reading a stale table: reading a non-existent silver_epi{year}results raises, and the DQ suite's schema/rowcount contracts follow the same parameter.",
    "spec_v1 \u00a7 Data Sources #2 is reconciled through /iterate \u2014 it states the naming convention rather than the 2024 instance, and records whether multi-vintage is supported or single-vintage is the deliberate design.",
    "[Erik/Fabric] a run with the current vintage still produces identical fact_epi_score output (this is a refactor, not a behavior change)."
  ],
  "dependencies": [
    "task-028"
  ],
  "decision_dependencies": [],
  "notes": [
    "CREATED 2026-07-23 by /work from task-028's verify-agent friction FR-021 and implement-agent issues_discovered. Not a defect in task-028 \u2014 that task's AC6 scoped only bronze_ingest_epi and silver-to-gold2, both of which it met.",
    "Blocked-ish on a spec decision: parameterizing bronze-to-silver may be the wrong answer if single-vintage EPI is deliberate. Resolve that in /iterate before writing code.",
    "FR-021 RESOLVED 2026-07-23 by /iterate \u2014 the SPEC half is done: \u00a7 Data Sources #2 now documents the bronze_epi{year}results / silver_epi{year}results convention, names p_epi_year and EPI_YEAR as the deriving parameters, and carries the single-vintage caveat. This task's remaining scope is the CODE half: parameterize bronze-to-silver.Notebook (hardcodes both names at lines 74/108). ADDED SCOPE found while grounding the spec edit \u2014 data_quality_checks.Notebook's BLOCKING_CHECKS contains a hardcoded ('schema_validation','oem_lh.silver_epi2024results') entry, and per that set's own comment a stale entry does NOT error, it silently demotes the check to advisory. Parameterizing the notebooks without fixing that entry would quietly drop a blocking check.",
    "CODE HALF AUTHORED 2026-07-23 (inline by /work, no agent). Criteria 1+2 done in code: bronze-to-silver.Notebook derives both EPI names from a new p_epi_year='2024' in its existing PARAMETERS CELL (lines 78/112); data_quality_checks.Notebook declares EPI_YEAR=2024 once (after DB config) and derives ALL 6 EPI table-name literals from it incl. the BLOCKING_CHECKS schema_validation entry (the stale-demotion trap). No notebook hardcodes a vintage literal in an EPI table name; a wrong vintage now fails loudly on a missing silver_epi{year}results. All 37 code cells across the 3 edited notebooks parse clean (ast). Criterion 3 already satisfied (spec \u00a7 Data Sources #2 reconciled via /iterate, FR-021). RESIDUAL (criterion 1 'exactly one source' cross-notebook): pipeline must pass one p_epi_year to all four notebooks, which needs parameter-cell toggles (Fabric UI) on silver-to-gold2 + DQ \u2014 Erik's half. Criterion 4 (re-run, identical fact_epi_score) is Erik/Fabric. See workspace/handoff-2026-07-23-fabric.md. NOTE: edits uncommitted pending Erik's go-ahead.",
    "CLOSED 2026-07-26. Two defects surfaced and were fixed during the wiring, both latent before the repoint: (1) bronze-to-silver's silver_epi write needed overwriteSchema -- bronze_ingest_epi types EPI as double where the retired EPI_file2table dataflow landed it as text, so mode('overwrite') failed with DELTA_FAILED_TO_MERGE_FIELDS (fix d2e80c1; verified value-neutral by joining silver to the new bronze on code -- silver EPI equals bronze EPI.new exactly, matches no EPI.old). (2) Toggling EPI_YEAR's own parameters cell in silver-to-gold2 MOVED the single permitted marker off p_full_load/p_from_date, silently pinning gold to an incremental load regardless of what the pipeline passed; all three assignments consolidated into one parameters cell (fix 12e2b19). Defect 2 was invisible in run 4bc9e635 because the run used the same values as the hardcoded defaults -- it would only have diverged on a full-load run."
  ],
  "user_feedback": null,
  "created_date": "2026-07-23",
  "updated_date": "2026-07-26",
  "completion_date": "2026-07-26",
  "verification_attempts": 1,
  "task_verification": {
    "result": "pass",
    "timestamp": "2026-07-26T18:05:00Z",
    "checks": {
      "spec_alignment": "pass",
      "artifact_inspection": "pass",
      "runtime_validation": "partial"
    },
    "verified_by": "user + orchestrator",
    "notes": "AC1 single source: pipeline parameter p_epi_year feeds all four EPI notebooks -- bronze_EPI and bronze-to-silver take it as a string, silver-to-gold and data_quality_checks take @int(pipeline().parameters.p_epi_year) as EPI_YEAR. No notebook hardcodes a year in a table name: every construction interpolates (bronze_ingest_epi:81-82 URL and table, bronze-to-silver:78,118, silver-to-gold2:69,988, data_quality_checks:266,357,377,494,600,1935). The only 2024 literals are the four parameter defaults. AC2 fail-loud: STRUCTURAL, not live -- no interpolation path can read a stale vintage, a missing table raises from spark.table/spark.sql, and bronze_ingest_epi raises earlier on the Yale 404; the DQ suite's rowcount, schema and blocking contracts all derive from the same EPI_YEAR. A live wrong-vintage run was deliberately NOT performed (would risk a partial 2025 load if Yale publishes that file). AC3: spec_v1 s Data Sources #2 reconciled via /iterate 2026-07-23 (FR-021). AC4: run 4bc9e635 green; fact_epi_score identical at 180 / 8450.2000 / 937166342, user-confirmed.",
    "issues": []
  },
  "verification_history": [],
  "test_protocol": null,
  "user_review_pending": false,
  "out_of_spec": false,
  "absorbed_into": null,
  "interaction_hint": "dashboard",
  "recovery_state": null
}
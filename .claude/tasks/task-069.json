{
  "spec_version": "spec_v1",
  "spec_fingerprint": "sha256:0bb0aad5d1a442b888672f543db310e4399b6a88a8f578ee3ea24d9f4cee0ded",
  "created_date": "2026-08-12",
  "updated_date": "2026-08-12",
  "completion_date": "2026-08-12",
  "verification_attempts": 1,
  "absorbed_into": null,
  "subtasks": [],
  "decision_dependencies": [
    "DEC-016"
  ],
  "user_feedback": "GUIDED WALKTHROUGH COMPLETE (Erik, 2026-08-12, CLI-direct). Every acceptance criterion closed on LIVE evidence.\n\nSEQUENCE RUN: deploy (push a2e7f6d -> workflow 31610840468 green, PUBLISH mode dry_run=false; both parameter badges intact) -> p_full_load=true run (green) -> repo DDL + seed against live Azure SQL (recordsAffected 132, THROW guard did not fire) -> NORMAL incremental run (green, all 10 activities) -> oem_report render check.\n\nMEASURED:\n  bronze_procurement_transactional: Quantity DecimalType(18,2), UnitPriceEUR DecimalType(18,2) -> AC3 closed by READING the landed Delta schema after a real Copy run, not by inference.\n  silver_procurement: quantity DecimalType(18,2), unitpriceeur DecimalType(18,2).\n  fact_procurement: quantity_base / unitprice_eur / spend_eur ALL DoubleType -> B-minimal held; TMDL untouched and still valid; the decimal(37,4) DirectLake risk never materialised because gold was pinned.\n  gold_low_confidence_audit.spend_impact: DoubleType -> FR-089's union-coercion reasoning confirmed live.\n  dq_data_type_consistency: 100.0/pass at 15:28:48Z (full-load) AND 16:06:16Z (incremental), against 75.0/fail at 04:16:47Z and 00:36:20Z the same day. THE SECOND READING IS THE LOAD-BEARING ONE — it proves the append path, not just the schema rewrite, produces conforming types.\n  SUM(spend_eur) = 3273776.03 on 132 rows — IDENTICAL to the task-024 baseline. Identity is the CORRECT outcome, not a coincidence: the 2026-08-12 re-seed had already written clean 2dp literals, so only sub-cent representation error was removed and it rounds away. Precision improved, semantics did not move.\n  bronze.UnitPriceEUR >2dp count: 0 of 132 (was 124 of 132) -> closes FR-088.\n  bronze_load_metadata: full-load 15:16:48Z rows_loaded=132 SUCCESS; incremental 15:53:49Z rows_loaded=11 SUCCESS; no FAILED rows. THE 11 IS THE PROOF the append path actually ran — a value near 132 would have meant it silently full-loaded and the thing under test was never exercised.\n  oem_report: renders 3.27M Total Spend EUR / 132 transactions / 11 materials; DirectLake re-framed against the rewritten Delta files without issue.\n\nPROTOCOL STEP 4 (a second incremental for steady state) DELIBERATELY SKIPPED — the daily 06:00 Europe/Stockholm schedule exercises exactly that path for free on the next firing. Erik agreed. Not a gap: one green incremental against the retyped source is already the evidence AC2 required.\n\nTHREE ORCHESTRATOR DEFECTS HIT DURING THE WALKTHROUGH, all mine, all cheap but all avoidable — recorded in notes: (1) wrong gold fact table name (gold_fact_procurement vs fact_procurement); (2) the test_protocol was never persisted to this file by the post-verification write, so it existed only in session context; (3) a bare SQL block handed to a PySpark cell, and a bronze_load_metadata query ordered by the watermark date instead of load_timestamp, which returned an arbitrary slice that showed nothing about the run under test.\n\nTHE PIPELINE'S tmp_task064_seed SCRIPT HELD THE ROLLBACK DDL (Quantity SMALLINT / UnitPriceEUR FLOAT), not the repo's DECIMAL(18,2). Running it unmodified would have silently undone the fix while reporting success. Caught because the script content was read before running rather than assumed — the handoff had said the rollback DDL lived only in a session scratch file, which was wrong.",
  "source": "Live regression during task-064's step-7 guided walkthrough, 2026-08-12. Rolled back operationally; the repo-vs-live contract is unresolved.",
  "id": "task-069",
  "title": "Reconcile azure/procurement.sql column types with the live table — the repo rebuild currently breaks bronze→silver",
  "status": "Finished",
  "priority": "high",
  "difficulty": 5,
  "owner": "both",
  "phase": "5",
  "spec_section": "Data Architecture",
  "out_of_spec": true,
  "out_of_spec_approved": true,
  "dependencies": [],
  "files_affected": [
    "fabric/bronze_to_silver.Notebook/notebook-content.py",
    "fabric/silver_to_gold.Notebook/notebook-content.py",
    "fabric/data_quality_checks.Notebook/notebook-content.py",
    "azure/procurement.sql",
    "azure/procurement_seed.sql",
    "docs/schemas/bronze_tables.md",
    "docs/data_quality_framework.md",
    ".claude/support/reference/merge-queue.md",
    ".claude/support/decisions/decision-015-azure-sql-seed-strategy.md"
  ],
  "description": "task-064 made the Azure SQL source rebuildable from the repo. Running that rebuild on 2026-08-12 BROKE THE PIPELINE, and the repo is still in the state that reproduces the outage.\n\nWHAT HAPPENED. azure/procurement.sql declares Quantity and UnitPriceEUR as DECIMAL(18,2), matching the spec and docs/schemas/bronze_tables.md. The live dbo.procurement_transactional it replaced was hand-created outside the repo with different types — recovered from bronze Delta v101 time travel as Quantity `short` and UnitPriceEUR `double`. Rebuilding from the repo therefore changed bronze's Delta schema to decimal(18,2), and bronze_to_silver could no longer write into silver_procurement, whose columns are still short/double: [DELTA_FAILED_TO_MERGE_FIELDS] Failed to merge fields 'quantity' and 'quantity', 3 consecutive failures.\n\nNote DEC-015 is WRONG on one point that matters for any fix: it calls the live column 'a 4-byte float'. It is a double. The 18.670000076293945 noise is float32 error baked into values stored in an 8-byte column. T-SQL REAL (= FLOAT(24), 4-byte) would land as Spark `float` and still mismatch; only FLOAT (= FLOAT(53)) lands as `double`.\n\nCURRENT STATE. Operationally rolled back: the live table was recreated as Quantity SMALLINT / UnitPriceEUR FLOAT, re-seeded from azure/procurement_seed.sql, and the pipeline is green end to end (bronze 132/11, silver 132, fact_procurement 132, silver dates 2024-01-31..2024-12-31). The rollback DDL exists ONLY as a scratch file from that session — it is NOT in the repo. So azure/procurement.sql now disagrees with the live table it is supposed to reproduce, and re-running it repeats the outage.\n\nTHE DECISION THIS TASK CARRIES. Two defensible directions, and they differ in what they treat as authoritative:\n\n  (A) Change azure/procurement.sql to SMALLINT / FLOAT. The repo then reproduces the live table exactly and the pipeline is safe. Cost: it contradicts the DECIMAL(18,2) contract documented in the spec and docs/schemas/bronze_tables.md, so those need correcting too (spec half via /iterate, DEC-016) — and it enshrines what was probably an accident of hand-creation as the contract. It also keeps the float32 noise story alive in bronze.\n\n  (B) Keep DECIMAL(18,2) and make bronze_to_silver tolerate it. Cast quantity/unitpriceeur explicitly at the silver boundary so silver's schema stops inheriting the source's physical type, and add overwriteSchema to the full-load write at L538 (L560's append path needs the cast, not overwriteSchema). Then re-run the repo DDL for real. Cost: a code change plus a full load, and gold_fact_procurement may need the same treatment one layer down — verify before assuming it does not.\n\nB is the better engineering answer (a silver schema should not be a function of an upstream physical type) but it is the larger change and it moves live data types. A is smaller and truer to what is actually deployed. Do not pick silently — this is a real fork.\n\nWHATEVER IS CHOSEN, the MQ-002 spec text landed 2026-08-11 is currently overstated: it says the Azure rows are reproducible from the repo, which is true of the ROWS and false of the SCHEMA. That correction routes through /iterate.",
  "acceptanceCriteria": [
    "The direction (A or B) is chosen explicitly by Erik and recorded as a decision record, not resolved inline.",
    "After the change, running the repo's DDL+seed against the live database and then running orchestrator_pipeline_bronze_to_gold end to end leaves the pipeline GREEN — this must be demonstrated in Fabric, not argued locally. A SQLite/local pass is explicitly insufficient: that is exactly what missed the defect the first time.",
    "The declared types in azure/procurement.sql, docs/schemas/bronze_tables.md, and the spec agree with each other AND with the Delta schema that bronze actually lands. Prove the last one by reading the bronze table's schema after a real Copy run, not by inference.",
    "DEC-015's '4-byte float' claim is corrected to `double` wherever it appears, since a fix built on that premise (choosing REAL) would fail.",
    "The spec statement that the Azure rows are reproducible from the repo is either made true or narrowed to the rows-not-schema claim, drafted into the merge queue for /iterate (DEC-016) rather than edited directly.",
    "If direction B is chosen, gold_fact_procurement is checked for the same schema-merge exposure and the finding is reported either way."
  ],
  "notes": "Created 2026-08-12 by the orchestrator during task-064's guided walkthrough, immediately after the rollback restored service. NOT yet approved by Erik as out-of-spec work — out_of_spec_approved is false; confirm before dispatching.\n\nowner=both because the verification is a live Fabric pipeline run, which is Erik's half under the project's Fabric-UI/local-code split.\n\nThe rollback DDL used on the night is reproducible from this description (Quantity SMALLINT, UnitPriceEUR FLOAT, everything else unchanged from azure/procurement.sql) — regenerate it rather than hunting for the scratch file, which lives in a session-scoped temp directory and will not survive.\n\nStanding hazard this instantiates: local verification exercises no Delta writer, so column-type and column-name defects are invisible until the Fabric run. Same family as the DELTA_INVALID_CHARACTERS_IN_COLUMN_NAMES gotcha in root CLAUDE.md.\n\n2026-08-12 — OUT-OF-SPEC APPROVED by Erik. Opened, then BLOCKED on DEC-016 (.claude/support/decisions/decision-016-bronze-silver-column-type-contract.md), which AC1 requires. research-agent populated the record; Erik selects.\n\nTHE FORK GREW A THIRD OPTION AND THE RESEARCH CHANGED WHAT THE ORIGINAL TWO MEAN:\n  - Direction B AS FRAMED IN THIS TASK IS THE MIGRATION ERIK ALREADY DECLINED on 2026-08-06 (task-059's documented acceptance) — steps 2-5 of the 5-step path at docs/data_quality_framework.md:1181-1187, in full. Scope unchanged; only the justification is new (a reproducibility-contract failure rather than a precision preference). Re-presenting it as new work would have been the failure mode.\n  - B-MINIMAL is new and is the only formulation genuinely narrower than what was declined: cast at silver AND pin gold back to double (silver_to_gold L1673 spend_eur_expr, L1832 unitprice_eur), so ONE layer moves instead of three, the TMDL is untouched, and migration step 4 becomes a verified no-op.\n  - DIRECTION B CARRIES AN UNQUANTIFIED DIRECTLAKE RISK the task never named: spend_eur becomes decimal(18,2) x decimal(18,2) = decimal(37,4) by Spark's multiply rule, against Power BI's fixed-decimal decimal(19,4). Representability unverified, invisible to every local test, and it would surface in the report layer only AFTER both notebooks succeeded. B-minimal eliminates it.\n  - DIRECTION A IS THE LARGER DOCUMENTATION CHANGE, ~3:1 (~11 line-ranges across 7 files incl. two gated spec lines), and it does not merely leave data_type_consistency failing at 75.00 — it RETIRES THE INTENT the check asserts, while the notebook's own do-not-relabel block forbids the obvious escape. It also converts the latent 16-bit quantity ceiling (32,767 vs observed max 3,293) into a deliberately chosen contract.\n  - A'S STATED COST MAY ALREADY BE FALSE: 'keeps the float32 noise story alive' — the 2026-08-12 re-seed wrote clean 2dp literals into a FLOAT(53) column, so the visible noise is likely already gone and § 7.3:1171's '124 of 132' is likely stale. One live read (L1) settles it.\n\nCORRECTIONS TO THIS TASK'S OWN FRAMING (from research): the fix at L538 is incomplete — the first-load branch at L543 is a SECOND unprotected overwrite in the same try block. gold_fact_procurement's exposure is partial, not total: quantity_base is already insulated by an explicit .cast('double') at L1779-1780; only unitprice_eur and spend_eur are exposed, and gold's full-load path already carries overwriteSchema (write_tbl L104), so no gold code change is required for the WRITE to succeed — only for the TYPES to be pinned.\n\nSCOPE GAPS FILED: FR-086 (AC4 has FIVE sites, none in files_affected — and it is a PRECONDITION for direction A, not a follow-up, since a DDL built on DEC-015's premise would say REAL and break silver from the opposite side), FR-087 (docs/architecture/data_sources.md:23,25 absent from files_affected), FR-088 (§ 7.3:1171 measurement stale). files_affected deliberately NOT widened yet — A and B have very different blast radii, so it is re-derived once the direction is chosen.\n\n2026-08-12 — CLAUDE'S HALF IMPLEMENTED AND VERIFIED (pass, attempt 1). DEC-016 = Option B-minimal, chosen by Erik with the research payload in hand. 9 files, +233/-57, of which exactly FIVE are code lines:\n  1-2. bronze_to_silver: .cast('decimal(18,2)') on quantity and unitpriceeur at the silver boundary — silver's schema is now DECLARED, not inherited from an upstream physical type. That inheritance was the actual defect.\n  3-4. bronze_to_silver: overwriteSchema on BOTH overwrite branches (L577 full-load, L582 first-load). The first-load branch is the one this task's own description missed; leaving it would have kept a latent repeat.\n  5. silver_to_gold: (quantity * unitpriceeur).cast('double') at L1689 and unitpriceeur.cast('double') at L1852.\nTHE APPEND PATHS DELIBERATELY GOT NO mergeSchema — adding it would re-hide exactly the drift this task exists to expose. Three new comments say so in place, so a future maintainer does not 'fix' it that way.\n\nWHY THE CAST ORDERING MATTERS AND WAS CHECKED SPECIFICALLY: the multiplication happens in decimal and only the RESULT is cast. The operand-cast form is visually near-identical in a diff. Measured on 3293.00 x 17.31 (exact 57001.8300): shipped form 57001.83 (err 0.0); operand-cast 57001.829999999994; the pre-change float32-image path 57001.82824134827 (err 1.76e-03).\n\nTMDL UNTOUCHED — zero diff lines. That is the whole difference between B-minimal and the three-layer B, and it is what keeps the unverified decimal(37,4)-vs-Power-BI-fixed-decimal(19,4) DirectLake risk off the table entirely.\n\nTHE QUALITY CHECK WAS EARNED, NOT SILENCED: type_consistency_checks still expects 'decimal' for both columns, byte-identical. The layer moved to meet the expectation. The do-not-relabel block survives and was rewritten forward-looking ('If this check regresses below 100.0, the layer moved — fix bronze_to_silver, not this dict').\n\nAC4 CLOSED AT SIX SITES, not the five DEC-016 enumerated — the implementer found a sixth identical stale claim in validate_data_type_consistency's own docstring, which only reading around the named line range would catch. Verifier swept for a seventh and found none.\n\nAC6 EXTENDED: gold_low_confidence_audit.spend_impact stays double only because unionByName coerces the decimal(38,4) branch against a CAST(NULL AS DOUBLE) branch — measured, schema-deterministic, so no change needed, but it is luck rather than a pin. Filed as FR-089.\n\nAC5 closed on the orchestrator side after verification flagged it open: MQ-005 parked in merge-queue.md, GATED so it cannot drain before AC2 closes.\n\nREMAINING: Erik's half — a 4-step Fabric run plus 8 measurements (test_protocol). THE ORDER IS LOAD-BEARING: deploy the notebooks, run p_full_load=true, THEN run the DDL, then run normally. Running the DDL before the notebook reproduces the 2026-08-12 outage.\n\n2026-08-12 WALKTHROUGH CORRECTION: the verify-agent's test_protocol named the gold fact table `gold_fact_procurement`; the real table is `fact_procurement` (silver_to_gold L1884 `write_tbl(fact_procurement_complete, \"fact_procurement\")`). The `gold_` prefix is NOT uniform in this lakehouse — dimensions and audit tables carry it (gold_dim_country, gold_unmapped_procurement_audit, gold_quality_history) while fact and mapping tables do not (fact_procurement, fact_epi_score, fact_supply_share, mapping_country_aliases_confidence). The research report had quoted the correct name; the protocol regularised it by assumption. Corrected in test_protocol above so a re-run does not misfire. Cost: one failed cell during Erik's walkthrough.\n\n2026-08-12 WALKTHROUGH CORRECTIONS (two orchestrator defects, both caught by Erik hitting them):\n  (1) TABLE NAME. The verify-agent's protocol named the gold fact table `gold_fact_procurement`; the real table is `fact_procurement` (silver_to_gold L1884 `write_tbl(fact_procurement_complete, \"fact_procurement\")`). The `gold_` prefix is NOT uniform in this lakehouse — dimensions and audit/quality tables carry it (gold_dim_country, gold_unmapped_procurement_audit, gold_quality_history) while fact and mapping tables do not (fact_procurement, fact_epi_score, fact_supply_share, mapping_country_aliases_confidence). The research report had quoted the correct name; the protocol regularised it by assumption. Cost: one failed cell mid-walkthrough.\n  (2) PROTOCOL NOT PERSISTED. The orchestrator's post-verification write set task_verification, verification_history, notes, user_review_pending and interaction_hint but DROPPED the test_protocol, so Erik's Fabric procedure existed only in session context and would have been lost at compaction — on a task whose entire remaining half is that procedure. Now persisted in full, with step statuses recorded as they complete.\n  (3) LAKEHOUSE ATTACH. A fresh notebook has no default context; `oem_lh.<table>` raises UnsupportedOperationException 'No default context found'. Attach the oem_lh LAKEHOUSE (not the identically-named SQL analytics endpoint, which is type Warehouse). Added to the protocol's preconditions.",
  "user_review_pending": false,
  "interaction_hint": "cli_direct",
  "task_verification": {
    "result": "pass",
    "scope": "Claude's half only — AC2 (live Fabric green run) and AC3's landed-Delta-schema conjunct remain Erik's half and are NOT claimed",
    "verified_date": "2026-08-12",
    "timestamp": "2026-08-12T15:00:17Z",
    "attempt_number": 1,
    "verifier": "verify-agent (separate context)",
    "checks": {
      "files_exist": "pass",
      "consistency_check": "pass",
      "spec_alignment": "pass",
      "output_quality": "pass",
      "runtime_validation": "partial",
      "integration_ready": "pass",
      "scope_validation": "pass"
    },
    "summary": "Verified against DEC-016 § Decision (B-minimal), treated as authoritative over task-069's pre-research A-vs-B framing. Every load-bearing claim re-derived independently. CAST ORDERING CORRECT — L1689 ships (a*b).cast('double'), the RESULT form, not the operand form; measured on 3293.00 x 17.31 (exact 57001.8300): shipped 57001.83 err 0.0; operand-cast 57001.829999999994; pre-change float32-image path 57001.82824134827 err 1.76e-03. TMDL UNTOUCHED — zero diff lines under fabric/OEMInsightBI.SemanticModel/; unitprice_eur L89, spend_eur L100, quantity_base L78 all still dataType: double. This is what makes it B-minimal rather than B. BOTH overwrite branches protected (L577 full-load, L582 first-load); append paths byte-unchanged and got NO mergeSchema (grep: 'mergeSchema' appears at exactly 3 sites, all new comment prose explaining why it must not be added; positive control overwriteSchema at 14 sites). DO-NOT-RELABEL BLOCK SURVIVES AND IS STRENGTHENED — type_consistency_checks still expects 'decimal' for both columns, byte-identical (unchanged context lines in the diff). The layer was fixed and the expectation held fixed: the opposite of a Root-Cause-Over-Symptom violation. TYPE ALGEBRA re-derived on a real local SparkSession with spark.sql.ansi.enabled=false (confirmed in-session): decimal(18,2)*decimal(18,2) -> decimal(37,4); (a*b).cast('double') -> double. AC4 6/6 sites correct with no 7th (repo-wide case-insensitive sweep over 13 matching files, every match read individually; remaining hits are the corrections themselves, DEC-016's explanations, task-064.json which already stated it correctly, and the FR bug reports quoting the error). AC6 EXTENDED beyond the reported finding: gold_low_confidence_audit.spend_impact confirmed by measurement to stay double via unionByName coercion against a CAST(NULL AS DOUBLE) branch — schema-deterministic, so no code change needed, but unpinned. All other consumers safe. PARITY CONTRACT verified not assumed — edits are inline cell code plus docstring/comment only; validate_data_type_consistency's body unchanged; no src/transformations/ movement required. TESTS 300 passed / 0 failed; ast.parse clean on all three edited notebooks. DOCS HONEST — § 7 records 75.00 -> 100.00 as an expectation with an explicit 'not yet a measurement' banner, and the '124 of 132' figure is marked STALE with the predicted value and the settling SQL rather than deleted or silently updated. No doc asserts a live outcome that has not happened.",
    "evidence": [
      {
        "type": "static_analysis",
        "assertion": "spend_eur casts the RESULT of the multiplication, not the operands",
        "outcome": "confirmed",
        "detail": "L1689 (a*b).cast('double'); measured err 0.0 vs 1.76e-03 on the pre-change path"
      },
      {
        "type": "static_analysis",
        "assertion": "fact_procurement.tmdl has zero diff lines",
        "outcome": "confirmed",
        "detail": "git diff --stat over the SemanticModel path is empty; all three columns still dataType: double"
      },
      {
        "type": "spark_execution",
        "assertion": "decimal(18,2)*decimal(18,2) -> decimal(37,4); .cast('double') pins it",
        "outcome": "confirmed",
        "detail": "local SparkSession, ANSI disabled to match Fabric, conf verified in-session"
      },
      {
        "type": "spark_execution",
        "assertion": "gold_low_confidence_audit.spend_impact stays double via unionByName coercion",
        "outcome": "confirmed",
        "detail": "SUM(decimal product) -> decimal(38,4) alone; unionByName against CAST(NULL AS DOUBLE) -> double"
      },
      {
        "type": "test_suite",
        "assertion": "pytest baseline preserved",
        "outcome": "confirmed",
        "detail": "300 passed, 0 failed"
      },
      {
        "type": "static_analysis",
        "assertion": "type_consistency_checks expectation dict byte-identical",
        "outcome": "confirmed",
        "detail": "both entries appear as unchanged context lines; do-not-relabel block intact and strengthened"
      }
    ],
    "residual": "AC2 OPEN by design — Erik's half. A green live Fabric run following the repo DDL re-run is the only evidence that closes it, and the Delta half is not merely untested but STRUCTURALLY UNREPRODUCIBLE locally (delta-spark not installed; local Spark never invokes a Delta writer, so DELTA_FAILED_TO_MERGE_FIELDS cannot be raised). AC3's third conjunct (bronze's landed Delta schema) is likewise live-only and must be READ after a real Copy run, not inferred. Both are covered by the 4-step + 8-measurement test_protocol. AC5's orchestrator half closed after verification: MQ-005 parked in merge-queue.md, GATED so it cannot drain before AC2 closes — DEC-016 chose the make-it-true branch, so draining early would swap one untrue spec statement for another in the opposite direction. Fallback narrowing wording carried in the entry if the DDL re-run is abandoned. MINOR, no change now: gold_low_confidence_audit.spend_impact is unpinned (safe by coercion, not by design) — removing or retyping the supply-share union branch would silently retype a DirectLake column."
  },
  "verification_history": [
    {
      "attempt": 1,
      "result": "pass",
      "timestamp": "2026-08-12T15:00:17Z",
      "verifier": "verify-agent",
      "summary": "Pass on attempt 1 for Claude's half. An earlier dispatch of the same verification was cut off by a session usage limit after 4 tool calls; verification_attempts was deliberately NOT incremented for that, since an infrastructure interruption is not a verification signal."
    }
  ],
  "test_protocol": {
    "summary": "Erik's half of task-069 (AC2 + the read-it-don't-infer-it half of AC3). Four ordered pipeline actions, then eight measurements. THE ORDER IS LOAD-BEARING: running the DDL before the notebook deploy + full-load reproduces the 2026-08-12 outage.",
    "estimated_time": "30-45 minutes (four pipeline runs plus eight reads)",
    "mode": "guided_cli",
    "preconditions": [
      "P1. The three edited notebooks must be DEPLOYED before any run. Push to main IS a real publish (deploy-fabric.yml runs fabric_cicd.publish_all_items; no dry-run guard on push). DONE 2026-08-12: commit a2e7f6d pushed, run 31610840468 green, PUBLISH mode dry_run=false.",
      "P2. Confirm the parameter-cell badge survived the publish on bronze_to_silver and silver_to_gold. A lost badge means p_full_load is never injected and step 2 silently runs as an incremental — which looks like success until step 3 crashes. DONE 2026-08-12: both intact.",
      "P3. Read every schema check from a SPARK NOTEBOOK with the oem_lh LAKEHOUSE attached — not the SQL analytics endpoint. Two workspace items are named oem_lh; the other is the endpoint (type Warehouse) and attaching it breaks partial namespaces with UnsupportedOperationException 'No default context found'. The endpoint's schema cache also lags hours behind type changes and will show OLD types while reporting success.",
      "P4. TABLE NAMING — the `gold_` prefix is NOT uniform. Fact and mapping tables have NO prefix: fact_procurement, fact_epi_score, fact_supply_share, mapping_country_aliases_confidence. Dimensions and audit/quality tables DO: gold_dim_country, gold_unmapped_procurement_audit, gold_quality_history."
    ],
    "steps": [
      {
        "step": 1,
        "type": "interactive",
        "instruction": "Deploy: push to main so fabric-cicd publishes bronze_to_silver, silver_to_gold and data_quality_checks. Then confirm the parameter-cell badge on bronze_to_silver and silver_to_gold.",
        "expected": "Workflow green in PUBLISH mode (dry_run=false), items published in place with no duplicates, both parameter badges intact.",
        "status": "DONE 2026-08-12 — run 31610840468 green; badges confirmed by Erik."
      },
      {
        "step": 2,
        "type": "interactive",
        "instruction": "Run orchestrator_pipeline_bronze_to_gold with p_full_load = true. Leave all other parameters at default. Azure SQL is still SMALLINT/FLOAT at this point and that is CORRECT — the new cast makes the source's physical type irrelevant.",
        "expected": "All activities green. Row counts unchanged: bronze 132 / 11, silver 132, fact_procurement 132.",
        "status": "DONE 2026-08-12 — Erik reports pipeline green."
      },
      {
        "step": 3,
        "type": "interactive",
        "instruction": "NOW run the repo DDL for real: azure/procurement.sql followed IMMEDIATELY by azure/procurement_seed.sql. Never stop between them and never run the pipeline in between — procurement.sql DROPs the live table and it is empty until the seed lands. Then run orchestrator_pipeline_bronze_to_gold in NORMAL mode (p_full_load = false / unset).",
        "expected": "DDL + seed succeed (the connection needs WRITE permission, not just SELECT — a failure here points at permissions, not the script). The subsequent incremental run completes green with no DELTA_FAILED_TO_MERGE_FIELDS and no FAILED row in bronze_load_metadata.",
        "why_this_is_the_proof": "This is the run that crashed on 2026-08-12. It exercises the incremental mode('append') into a silver table whose schema must now match the newly-retyped bronze. A full-load run would hide it.",
        "if_it_fails": "Do NOT add mergeSchema to the append — that re-hides exactly the drift this change exists to expose. Re-run once with p_full_load = true to re-establish the schema, then retry the incremental.",
        "status": "DONE 2026-08-12 — tmp_task064_seed script edited from the rollback DDL (SMALLINT/FLOAT) to DECIMAL(18,2); ran DDL+seed (recordsAffected 132, THROW guard silent); NORMAL incremental run green, all 10 activities."
      },
      {
        "step": 4,
        "type": "interactive",
        "instruction": "Run the pipeline once more in normal incremental mode to prove steady state.",
        "expected": "Green; still 132 rows; no FAILED watermark row.",
        "status": "SKIPPED BY AGREEMENT — the daily 06:00 Europe/Stockholm schedule exercises the same path on its next firing. One green incremental against the retyped source already satisfies AC2."
      },
      {
        "step": 5,
        "type": "command",
        "instruction": "MEASUREMENT 1 (AC3 — read it, do not infer it). Run AFTER step 3.",
        "command": "spark.table('oem_lh.bronze_procurement_transactional').schema",
        "expected": "Quantity and UnitPriceEUR both decimal(18,2). This is the only evidence that closes AC3's third conjunct — a green pipeline alone does not.",
        "status": "CONFIRMED — Quantity DecimalType(18,2), UnitPriceEUR DecimalType(18,2). AC3 closed."
      },
      {
        "step": 6,
        "type": "command",
        "instruction": "MEASUREMENT 2 — silver schema.",
        "command": "spark.table('oem_lh.silver_procurement').schema",
        "expected": "quantity and unitpriceeur both DecimalType(18,2); the other six columns unchanged (date -> DateType, four descriptive columns -> StringType).",
        "status": "CONFIRMED 2026-08-12 after step 2 — quantity DecimalType(18,2), unitpriceeur DecimalType(18,2)."
      },
      {
        "step": 7,
        "type": "command",
        "instruction": "MEASUREMENT 3 — the whole point of B-minimal: gold must NOT have moved.",
        "command": "spark.table('oem_lh.fact_procurement').schema",
        "expected": "unitprice_eur DoubleType, spend_eur DoubleType, quantity_base DoubleType. If any reads DecimalType the pins did not deploy and fact_procurement.tmdl (L89/L100) is now out of sync with the lakehouse — STOP and report rather than editing the TMDL.",
        "status": "CONFIRMED — quantity_base / unitprice_eur / spend_eur all DoubleType. TMDL untouched."
      },
      {
        "step": 8,
        "type": "command",
        "instruction": "MEASUREMENT 4 — the check this task exists to earn.",
        "command": "SELECT refresh_timestamp, entity, metric_value, status FROM oem_lh.gold_quality_history WHERE metric_name = 'dq_data_type_consistency' ORDER BY refresh_timestamp DESC LIMIT 3",
        "expected": "metric_value = 100.00, status = pass, for silver_procurement (was 75.00 on every prior run). If below 100.00 the layer moved — fix bronze_to_silver, NEVER the expectation dict; the notebook's own do-not-relabel block forbids it.",
        "status": "CONFIRMED — 100.0/pass at 15:28:48Z and again at 16:06:16Z (incremental)."
      },
      {
        "step": 9,
        "type": "command",
        "instruction": "MEASUREMENT 5 — precision must move, semantics must not.",
        "command": "SELECT SUM(spend_eur) AS sum_spend_eur, COUNT(*) AS n FROM oem_lh.fact_procurement",
        "expected": "Compared to the task-024 baseline 3273776.03: movement of at most a few cents (recovered precision — the ~0.0018 per-row float32 error is gone). Movement beyond sub-cent means semantics changed, not precision — investigate, do not accept.",
        "status": "CONFIRMED — 3273776.03 / n=132, identical to the task-024 baseline."
      },
      {
        "step": 10,
        "type": "command",
        "instruction": "MEASUREMENT 6 — settles the stale '124 of 132' figure flagged at docs/data_quality_framework.md § 7.3 (DEC-016 question L1, FR-088).",
        "command": "SELECT COUNT(*) FROM oem_lh.bronze_procurement_transactional WHERE UnitPriceEUR <> ROUND(UnitPriceEUR, 2)",
        "expected": "0 of 132. If not 0, the re-seed prediction in azure/procurement_seed.sql's header is wrong and § 7.3 needs the real number rather than the predicted one.",
        "status": "CONFIRMED — 0 of 132 (was 124). Closes FR-088."
      },
      {
        "step": 11,
        "type": "command",
        "instruction": "MEASUREMENT 7 — the unpinned column flagged as FR-089.",
        "command": "spark.table('oem_lh.gold_low_confidence_audit').schema",
        "expected": "spend_impact still DoubleType (matching gold_low_confidence_audit.tmdl L92). Confirmed locally that unionByName coerces it, so this should hold — a cheap confirmation of a coincidence rather than a pin.",
        "status": "CONFIRMED — spend_impact DoubleType."
      },
      {
        "step": 12,
        "type": "command",
        "instruction": "MEASUREMENT 8 — no silent failure hiding behind a green pipeline.",
        "command": "SELECT * FROM oem_lh.bronze_load_metadata ORDER BY last_load_date DESC LIMIT 10",
        "expected": "No FAILED rows written during steps 2-4; the SUCCESS watermark advanced to the run's max date.",
        "status": "CONFIRMED — full-load 132 SUCCESS, incremental 11 SUCCESS, no FAILED rows."
      },
      {
        "step": 13,
        "type": "interactive",
        "instruction": "Open oem_report and confirm Total Spend EUR renders.",
        "expected": "Renders normally. DirectLake type-mismatch failures surface ONLY in the report layer, after both notebooks have already reported success.",
        "status": "CONFIRMED — oem_report renders 3.27M / 132 / 11; DirectLake re-framed cleanly."
      }
    ],
    "automated_results": "Local verification completed everything locally completable: 300/300 pytest, ast.parse clean on all three edited notebooks, and the full type-promotion algebra re-derived on a real SparkSession with ANSI disabled to match Fabric (decimal(18,2)*decimal(18,2) -> decimal(37,4); result-cast -> double, exact to the cent; SUM(decimal product) unioned against CAST(NULL AS DOUBLE) -> double). The Delta/lakehouse half is not merely untested but STRUCTURALLY UNREPRODUCIBLE locally — delta-spark is not installed and local Spark never invokes a Delta writer — so these steps are the only evidence that can close AC2 and AC3's third conjunct.",
    "outcome": "ALL STEPS CLOSED 2026-08-12. AC2 and AC3 satisfied on live Fabric evidence."
  }
}

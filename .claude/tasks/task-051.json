{
  "priority": "P2",
  "owner": "claude",
  "phase": "2",
  "phase_name": "Silver / Gold Transformations & Data Quality",
  "spec_fingerprint": "sha256:0e1cb6c1bbfcc356721b8b7ad47b1a6083b49882154ca4219a6b88276228498b",
  "spec_version": "1",
  "section_snapshot_ref": "",
  "estimated_hours": 1,
  "dependencies": [],
  "decision_dependencies": [],
  "user_feedback": "",
  "created_date": "2026-08-03",
  "updated_date": "2026-08-03",
  "completion_date": null,
  "verification_attempts": 0,
  "verification_history": [],
  "task_verification": {},
  "test_protocol": null,
  "user_review_pending": false,
  "recovery_state": "",
  "out_of_spec": false,
  "absorbed_into": "",
  "interaction_hint": "dashboard",
  "status": "Awaiting Verification",
  "difficulty": 3,
  "id": "task-051",
  "title": "Fix pipeline_error_handler reporting a retried-then-succeeded activity as a run failure",
  "spec_section": "Orchestration",
  "section_fingerprint": "sha256:317baa48f168d3c18e541f60bae2e642f8ebf35f89bf9f26be7c4ec8b285357d",
  "description": "pipeline_error_handler re-raised whenever any harvested activity row carried status Failed. queryactivityruns returns one row PER ATTEMPT, so an activity the pipeline retried into success appears as N Failed rows plus one Succeeded row \u2014 and the run was reported Failed even though it completed green. Observed on the 2026-08-03 14:08 orchestrator run: bronze_WGI failed twice against a degraded World Bank API and succeeded on its third attempt; the run was functionally green end to end yet ended red. retryAttempt is always null on this endpoint, so the final attempt has to be derived by ranking activityRunStart \u2014 the same signal derive_retry_attempt already uses. Left unfixed this becomes a false-alarm generator the moment task-010 puts the pipeline on a schedule, which is the failure mode that trains people to ignore alerts.",
  "files_affected": [
    "fabric/pipeline_error_handler.Notebook/notebook-content.py",
    "tests/test_retry_ordinal.py"
  ],
  "acceptanceCriteria": [
    "An activity that failed one or more attempts and then succeeded does NOT fail the pipeline run.",
    "An activity whose final attempt failed DOES still fail the run, so task-026's DQ gate and DEC-004's failure propagation are preserved.",
    "Every attempt is still written to gold_pipeline_execution_log, including the failed attempts of a recovered activity \u2014 get_retry_effectiveness() reads exactly that per-attempt detail.",
    "The final attempt is identified by ranking activityRunStart, not by reading retryAttempt (which queryactivityruns always returns as null).",
    "Recovered activities are reported in the notebook output so a retry storm stays visible rather than silent.",
    "The decision logic is a pure function extracted and tested from the live notebook per the reference-implementation contract."
  ],
  "notes": [
    "CREATED 2026-08-03 (/work, Erik-approved). Carried as an open question on the dashboard ('No decision taken \u2014 say the word and I'll open a task') since the 14:08 run; Erik chose to fix it together with task-052.",
    "IMPLEMENTED 2026-08-03 (inline, not via implement-agent \u2014 session-level instruction is not to dispatch subagents unless asked). Added summarize_final_outcomes(activities, self_activity_name) beside derive_retry_attempt: it collapses per-attempt rows to one final terminal outcome per activity by max(activityRunStart), returning (final_failures, recovered). harvest_pipeline_run now raises on final_failures instead of on any FAILED row, prints recovered activities, and writes rows exactly as before. Cell header updated with the corrected semantics.",
    "VERIFIED LOCALLY: ast.parse OK; 8 CELL markers intact (1 PARAMETERS + 7 CELL, unchanged); 14 new tests in tests/test_retry_ordinal.py loading the function from the live notebook via ast (with _TERMINAL_STATUS single-sourced from the notebook too); 249/249 pytest pass. NEGATIVE CONTROL run explicitly: on the 14:08 row shape the old 'any Failed row' logic raises (2 failed rows) while the new logic returns no final failures and recovered=[('bronze_WGI', 2)] \u2014 so the tests are not vacuous.",
    "TEST DATA PROVENANCE: the three bronze_WGI attempt timestamps in WGI_ATTEMPTS are RECONSTRUCTED from the durations recorded in task-036's WGI RELIABILITY FINDING note (16m47s, 3m54s, 4m7s), not captured verbatim from the API \u2014 only their ordering is load-bearing. Flagged in the test file so it is not mistaken for the verbatim provenance COPY_STARTS carries.",
    "NOT DEPLOYED BY CLAUDE: push to main auto-publishes to the live workspace, so committing is Erik's call and follows the Fabric->local->Update ordering convention."
  ]
}

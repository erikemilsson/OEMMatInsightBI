{
  "id": "task-048",
  "status": "Awaiting Verification",
  "priority": "P1",
  "phase": "4",
  "phase_name": "CI/CD Deployment",
  "spec_section": "Orchestration",
  "spec_version": "1",
  "spec_fingerprint": "sha256:b966f6b2eb73d586281f197f9f62a2772b2e67fff1d3def6847c905491290d6b",
  "section_fingerprint": "sha256:14923ee96fe7a0954d378cca905df86036427e457a70b867bdf45fcdbd2cfc61",
  "section_snapshot_ref": "",
  "decision_dependencies": [],
  "out_of_spec": false,
  "absorbed_into": "",
  "created_date": "2026-07-31",
  "updated_date": "2026-07-31",
  "completion_date": null,
  "verification_attempts": 3,
  "interaction_hint": "cli_direct",
  "user_review_pending": false,
  "difficulty": 5,
  "owner": "both",
  "dependencies": [],
  "title": "Retire bronze_azureSQLdb2table \u2014 replace the last RefreshDataflow with a Copy activity so the pipeline is SPN-safe",
  "files_affected": [
    "fabric/orchestrator_pipeline_bronze_to_gold.DataPipeline/pipeline-content.json",
    "fabric/bronze-to-silver.Notebook/notebook-content.py",
    "fabric/parameter.yml",
    "src/transformations/procurement_dates.py",
    "tests/test_procurement_dates.py",
    ".claude/spec_v1.md",
    "README.md",
    ".claude/commands/run-bronze.md",
    "docs/architecture/fabric-artifacts-inventory.md",
    ".claude/support/documents/architecture/orchestration.md",
    ".claude/support/documents/architecture/data_sources.md",
    ".claude/support/documents/architecture/medallion_architecture.md",
    ".claude/support/documents/error_recovery_playbook.md",
    ".claude/support/documents/fabric_workspace.md",
    ".claude/support/documents/incremental_load_strategy.md",
    ".claude/support/retired/bronze-azuresqldb2table-dataflow/",
    ".claude/dashboard-state.json",
    ".claude/support/documents/standards/naming_standards.md",
    ".claude/commands/run-full-pipeline.md",
    "docs/setup/TROUBLESHOOTING.md",
    "docs/guides/pipeline_schedule.md",
    "docs/architecture/data-flow-diagram.md"
  ],
  "description": "Replace the pipeline's single remaining RefreshDataflow activity (bronze_procurement -> bronze_azureSQLdb2table) with two Copy activities reading dbo.procurement_transactional and dbo.supplier_ref from Azure SQL directly into bronze_procurement_transactional and bronze_supplier_ref. Retire the dataflow per .claude/rules/feature-retirement.md. This removes the last item in the pipeline that a service principal cannot refresh and that loses its credentials on every fabric-cicd publish.",
  "acceptanceCriteria": [
    "The pipeline's `bronze_procurement` RefreshDataflow activity is gone, replaced by Copy activity/activities producing the SAME two bronze tables with the SAME names: `bronze_procurement_transactional` and `bronze_supplier_ref`.",
    "The Copy source uses a Fabric Azure SQL connection (credential stored in Fabric's connection store, never in a tracked file) shared with the `Fabric-SPN-Access` security group \u2014 mirroring the two HttpServer connections that already run green under the SPN.",
    "Incremental behaviour is preserved: the `p_from_date` pipeline parameter still drives a 7-day look-back window via a source query, and `p_from_date = '1900-01-01'` still means full load. If this moves to a different mechanism, the change is documented and the parameter contract stays intact.",
    "The dataflow's `CorrectedDate` transformation \u2014 `#date(Day([Date])+2000, Month([Date]), Year([Date])-2000)`, a day/year swap correcting the source data \u2014 is relocated to bronze-to-silver, NOT dropped. A test or explicit verification shows the silver `Date` values are unchanged versus the pre-migration table.",
    "Bronze row counts for both tables match the pre-migration values (capture the baseline BEFORE the first post-change pipeline run \u2014 it is one-shot and self-destructing, as task-038_2 learned).",
    "`bronze_azureSQLdb2table` is retired per `.claude/rules/feature-retirement.md`: snapshot + manifest.json under `.claude/support/retired/`, commit pin, spec annotation, discoverability entry. Do NOT simply delete it.",
    "A full pipeline run completes green end to end, with no RefreshDataflow activity remaining and therefore no SPN-refresh restriction and no credential-unbinding exposure on future publishes."
  ],
  "notes": [
    "CREATED 2026-07-31. Root cause chain that produced this task is fully written up in task-046's notes \u2014 read those first. Short version: the fabric-cicd publish strips Dataflow Gen2 credentials, and once re-bound the refresh fails with `SPNBasedRefreshNotAllowed` (dataflow id 47f7a4ab-2b69-4275-8171-1f6bd55b25e4). Both failure modes recur on EVERY automated deploy, so this blocks DEC-007's push-to-main flip, not just task-046.",
    "MECHANISM CHOSEN BY ERIK 2026-07-31 (inline, after being shown the fork): Copy activity, NOT a notebook. Rationale: task-035's notebook migration worked for EPI/WGI because those sources are PUBLIC HTTP with no credential to store. Azure SQL needs a secret at runtime, and there is no JDBC or secret-retrieval pattern anywhere in the repo (probed with a positive control). Key Vault is unusable here \u2014 the Azure subscription lives in tenant a7d750d0 (live.com#erikemilsson@hotmail.com) while Fabric is a different tenant, the same split that 401s the Fabric token and breaks 'Browse Azure'. A pipeline parameter or in-notebook password would put a credential in a tracked file, violating a hard invariant. A Copy activity stores the credential in Fabric's own connection store \u2014 the exact pattern already proven green under the SPN by bronzecopy_EUSupplyShares and bronzecopy_GlobalSupplyShares.",
    "\u26a0\ufe0f SPEC AMENDMENT REQUIRED \u2014 DO NOT EDIT THE SPEC DIRECTLY. spec_v1.md \u00a7 Orchestration line 628 names activity #4 as `bronze_procurement | RefreshDataflow | bronze_procurement_transactional, bronze_supplier_ref | 3`. That row must change to Copy. Per DEC-016 this routes through /iterate; it is NOT part of this task's implementation and must not be done inline.",
    "SOURCE OF TRUTH FOR THE MIGRATION: fabric/bronze_azureSQLdb2table.Dataflow/mashup.pq. Three queries \u2014 `p_from_date` (parameter, default '1900-01-01'), `procurement_transactional` (dbo.procurement_transactional -> bronze_procurement_transactional), `supplier_ref` (dbo.supplier_ref -> bronze_supplier_ref). Server procurement-supplier.database.windows.net, database procurement-supplier-db, EnableCrossDatabaseFolding=true.",
    "THE DATE FIX IS THE SUBTLE PART. The mashup builds CorrectedDate as #date(Day+2000, Month, Year-2000) \u2014 a deliberate day/year swap correcting malformed source dates \u2014 then DROPS the original `Date` and renames CorrectedDate to `Date`. A Copy activity cannot transform, so this must move. Preferred home is bronze-to-silver (keeps bronze raw, which is better medallion practice than pushing it into the source query). Whichever is chosen, criterion 4 requires proving silver dates are unchanged.",
    "CREDENTIAL IS ALREADY VERIFIED WORKING: server admin login `erikdatabase`, password reset and confirmed 2026-07-31 via direct sqlcmd (SUSER_NAME()=erikdatabase, DB_NAME()=procurement-supplier-db). Do NOT use `erikdatabase2` \u2014 that is a stale contained-database user with a separate, unreset password, and it is the pre-filled value that caused several failed re-bind attempts.",
    "FIREWALL: a rule `erik-laptop` = 37.247.31.201/32 exists on the server for local diagnostics. It is dynamic and may need updating; delete when no longer needed via `az sql server firewall-rule delete --resource-group RG1 --server procurement-supplier --name erik-laptop`.",
    "ALSO FIX IN THIS TASK: fabric/parameter.yml currently documents the dataflow re-bind gotcha as if the fix were on the tenant-level 'Manage connections and gateways -> Data sources' page. That is WRONG and cost a diagnostic round \u2014 the Gen2 credential is bound inside the dataflow definition. Since the dataflow is being retired, replace that note with the Copy-activity connection guidance rather than merely correcting it.",
    "HEALTHY, DO NOT TOUCH: the 02:38 run had bronzecopy_EUSupplyShares (19s), bronzecopy_GlobalSupplyShares (20s), bronze_EPI (1m15s) and bronze_WGI (1m16s) all SUCCEEDED. Only bronze_procurement failed. task-035's notebook migration and both HttpServer connections are working.",
    "2026-07-31 PARTIAL. Local half implemented by implement-agent, which then terminated early on an API session limit (resets 06:30 Europe/Stockholm) without returning a structured report. Orchestrator re-verified the landed work directly rather than trusting narration: tests/test_procurement_dates.py = 21 passed; full suite = 210 passed, no regressions; notebook parses as valid Python; exactly one '# PARAMETERS CELL' marker (line 38), still ahead of the new def (396) and its use (444). conftest's spark fixture already sets spark.sql.ansi.enabled=false, so the local run matches Fabric's cast semantics.",
    "DESIGN FINDING (task-048, worth keeping): the relocated correction must run BEFORE the silver look-back window, not after. Everything downstream is expressed in CORRECTED-date space \u2014 bronze_load_metadata.last_load_date comes from silver_df.agg(F.max('date')), and window_min_date is the delete-insert boundary. Windowing raw dates against a corrected-space watermark would DELETE a range the append does not restore: silent data loss. The old SQL pushdown (\"WHERE Date >= '{lookback_str}'\") was therefore replaced with a DataFrame filter applied after correct_procurement_date. tests/test_procurement_dates.py::TestNotebookParity::test_notebook_corrects_before_windowing pins the ordering in the notebook TEXT, and TestIncrementalWindowEquivalence::test_windowing_on_raw_dates_is_wrong asserts the hazard still differs, so a future reorder fails locally instead of silently in Fabric.",
    "ENGINE-CONSUMER AUDIT 2026-07-31 (grep + positive control, per rules/agents.md 'Negative Findings Require a Positive Control'). bronze_azureSQLdb2table has 10 consumers: fabric/parameter.yml x2 (the KNOWN LIMITATION block ~47-59 AND a live key_value_replace at ~144-146 \u2014 the latter was NOT in the original task notes); pipeline-content.json (the bronze_procurement RefreshDataflow activity); .claude/spec_v1.md x4 (~150, ~628, ~828, ~991-993 \u2014 the task notes named only 628); README.md x2 (~31 and a mermaid node B1[bronze_procurement] at ~64); project_definition.md x5 (~143, ~635-637, ~847, ~1039); tests/test_retry_ordinal.py:51 (synthetic fixture keyed on the activity name 'bronze_procurement').",
    "FOURTH ERROR CATEGORY, 2026-07-31 \u2014 'DATABASE NOT CURRENTLY AVAILABLE' = SERVERLESS AUTO-PAUSE, NOT A CREDENTIAL PROBLEM. Creating the Fabric Azure SQL connection failed with Fabric's generic wrapper 'We could not access the data source. Please make sure you have permission ... and that your credentials are correct' (status 400) \u2014 but the Details block read 'Database procurement-supplier-db on server procurement-supplier.database.windows.net is not currently available. Please retry the connection later.' The wrapper is boilerplate and actively misleading; ALWAYS read the Details block. Proof it was auto-pause: `az sql db show -g RG1 -s procurement-supplier -n procurement-supplier-db` returns skuName GP_S_Gen5 (the S = serverless), autoPauseDelay 60 (minutes), status Online, pausedDate null, resumedDate 2026-07-31T11:20:00Z. The error timestamp was 13:19:19 +0200 = 11:19:19Z \u2014 i.e. the attempt hit a paused DB, TRIGGERED the resume, and failed during the ~41s resume window. Retrying after the resume succeeds. Extends the existing taxonomy: 'Login failed for user X' = credential bound and wrong; 'Credentials are required to connect / Challenge type: Resource' = no credential bound; 'Login timeout expired / TCP Provider' = never reached the server (firewall/port); 'Database ... is not currently available' = reached the server, database paused or resuming, says NOTHING about the credential.",
    "PIPELINE IMPLICATION OF autoPauseDelay=60 (assessed 2026-07-31, no change made): any pipeline run starting after >60 min of database inactivity will hit the resume window on its FIRST Copy attempt against Azure SQL. This is recurring, not one-off. Existing retry config already covers it \u2014 bronze_procurement carries retry=3 / retryIntervalInSeconds=300 (task-011), and a 5-minute retry interval comfortably exceeds the ~41s observed resume, so attempt 2 succeeds. The replacing Copy activities MUST inherit that retry policy; do not author them with retry=0. Disabling auto-pause (`az sql db update --auto-pause-delay -1`) would also fix it but bills continuously for a portfolio project, so it was deliberately NOT done. Note the first activity of a cold run may therefore always show one failed attempt in the run history \u2014 that is expected, not a regression.",
    "CONNECTION CREATED 2026-07-31 11:29:13Z. GUID da9667d6-9df0-492e-83e0-de4a55fc388e, displayName oem_azuresql_procurement, path 'procurement-supplier.database.windows.net;procurement-supplier-db', type SQL, credentialType Basic, privacyLevel Organizational, connectivityType ShareableCloud, connectionEncryption Encrypted, skipTestConnection false (so Fabric's own test-connection passed on create \u2014 the credential is proven). gatewayId 299a756b-b9b4-49ec-99ff-d462ca4e9ade. NOTE a SECOND, older SQL connection to the same server exists: ce4b380a-50f3-410c-adc5-65393d7d2f84 ('procurement-supplier.database.windows.net;procurement-supplier-db fabricuser') \u2014 presumably the dataflow's original binding. Use da9667d6 for the Copy activities; do not confuse the two.",
    "SHARING NOT YET DONE as of 2026-07-31: GET /v1/connections/da9667d6-.../roleAssignments returns exactly ONE entry, Owner = user fa4996c2-d865-4764-907c-b5685b770e2e. Fabric-SPN-Access (group object id dbe8361c-5810-4e35-8958-6d1b7cdd3680, resolved via `az ad group show --group Fabric-SPN-Access`) is NOT on it. Without that role assignment the SPN publish reproduces 'User does not have access to the connection used in the Pipeline' \u2014 the exact failure that took the first real publish to 15/16 (task-046 note 2).",
    "SHARING DONE 2026-07-31 (orchestrator, via REST, on Erik's explicit yes): POST /v1/connections/da9667d6-.../roleAssignments {\"principal\":{\"id\":\"dbe8361c-5810-4e35-8958-6d1b7cdd3680\",\"type\":\"Group\"},\"role\":\"User\"} -> HTTP 201. Independently re-verified with GET: 2 assignments now, Owner/User fa4996c2 (Erik) + User/Group dbe8361c (Fabric-SPN-Access). Matches how the two HttpServer connections are configured.",
    "\u2705 PRE-MIGRATION BRONZE BASELINE CAPTURED 2026-07-31 13:4x (acceptanceCriteria[4] \u2014 the one-shot, self-destructing measurement). bronze_procurement_transactional = 132 rows; bronze_supplier_ref = 11 rows. These are the numbers the post-migration Copy activities must reproduce exactly. METHOD (no Fabric UI, no notebook run \u2014 repeatable via .claude/support/workspace/delta-row-counts.py): read the Delta transaction log straight from OneLake over the DFS API with an `https://storage.azure.com/` token as the fabricuser account, seed active files from the _last_checkpoint parquet (version 40 for both tables, read with pyarrow), replay every later JSON commit applying add/remove, then sum stats.numRecords over the surviving add files. Procurement replayed commits 41-47 -> 2 active data files; supplier_ref replayed 41-45 -> 2 active data files. NOTE pyodbc is NOT installed and the local sqlcmd is the classic ODBC build (no --authentication-method), so the lakehouse SQL analytics endpoint (2binpjytvaeevef26xkmilpx4e-nxgojgodn2tutlwzw2nqjkl2ve.datawarehouse.fabric.microsoft.com) was NOT the route used; OneLake + pyarrow needs no extra install. Also note pyspark could not read the checkpoint parquet by local path (UNABLE_TO_INFER_SCHEMA) while pyarrow read the identical file fine \u2014 use pyarrow for Delta-log work.",
    "FABRIC GIT STATUS AUDITED 2026-07-31 via GET /v1/workspaces/{ws}/git/status (HTTP 200). workspaceHead == remoteCommitHash == 04ed718; 5 changes, ALL workspaceChange=Modified / remoteChange=None, zero conflicts: DataflowFabric bronze_azureSQLdb2table, DataflowFabric EPI_file2table, DataflowFabric WGI_file2table, Lakehouse oem_lh, Warehouse oem_wh. Cause: the three dataflows were opened in the editor during the 2026-07-31 credential re-bind; lakehouse/warehouse go dirty from ordinary table-state drift. GUIDANCE: do NOT press Update (git->workspace is what unbinds dataflow credentials), and do NOT commit these 5 \u2014 committing bronze_azureSQLdb2table would invalidate the retirement manifest's stated assumption that no commit between the 04ed718 pin and the removal touches the dataflow. When the Copy activities are built in the UI, commit ONLY the DataPipeline item.",
    "RETIREMENT SNAPSHOT FIDELITY CONFIRMED 2026-07-31 by diffing the LIVE workspace definition (POST /v1/workspaces/{ws}/items/47f7a4ab-2b69-4275-8171-1f6bd55b25e4/getDefinition) against the repo files the snapshot was taken from. mashup.pq is BYTE-IDENTICAL (3328 bytes, 9 CRLF, both sides) \u2014 so the CorrectedDate logic and the p_from_date/native-query contract in the snapshot are exactly what is running. .platform 'differs' only because getDefinition returns logicalId 00000000-0000-0000-0000-000000000000; the repo's d55b25e4-1f6b-8171-4275-2b6947f7a4ab is the byte-reordered form of the REST itemId, matching the known 'REST itemId != git logicalId' behaviour. Neither is a real drift.",
    "\u26a0\ufe0f SEPARATE FINDING, NOT BLOCKING task-048 \u2014 POSSIBLE PLACEHOLDER GUID COMMITTED IN THE REPO. queryMetadata.json differs live-vs-repo in exactly one field: p_from_date.queryId is 'a1b2c3d4-e5f6-7890-abcd-ef1234567890' in the REPO but '91f2f633-82c9-4f8e-b8e6-35e39df88285' LIVE. The repo value is one of the four 'fake-looking' GUIDs task-044's notes recorded as 'independently confirmed as REAL notebook logicalIds via .platform files'. It cannot be both a notebook logicalId and this dataflow's query id, so either that task-044 confirmation was wrong for this GUID or a placeholder was committed. Low impact here because the dataflow is being retired, but the SAME GUID may be load-bearing elsewhere (parameter.yml key_value_replace targets notebook logicalIds) \u2014 worth a targeted audit before trusting any of those four values. Recorded rather than acted on: out of task-048's scope.",
    "COPY ACTIVITIES BUILT BY ERIK IN THE FABRIC UI 2026-07-31 and audited by the orchestrator against the LIVE workspace definition (getDefinition on pipeline d44ac501-95f9-4d7a-b50e-3d02c55dee23): 0 failures, 0 warnings. bronzecopy_procurement_transactional (dbo.procurement_transactional -> bronze_procurement_transactional) and bronzecopy_supplier_ref (dbo.supplier_ref -> bronze_supplier_ref), both type=Copy, policy timeout 0.12:00:00 / retry 3 / retryIntervalInSeconds 300, sink LakehouseTableSink with tableActionOption=OverwriteSchema, both bound to connection da9667d6-9df0-492e-83e0-de4a55fc388e, both with empty dependsOn (parallel bronze stage). bronze_procurement RefreshDataflow is GONE and NO RefreshDataflow of any name remains in the pipeline \u2014 acceptanceCriteria[0] and the SPN-safety goal of acceptanceCriteria[6] are structurally met. 'bronze-to-silver data cleaning' correctly re-wired to SIX Succeeded dependencies incl. both new copies, with the stale bronze_procurement dep removed. NOTE: UI menu choice matters \u2014 'Add copy data activity' (a plain Copy activity), NOT 'Add copy job activity' (which creates a separate Copy Job ITEM, a different Fabric item type that would add a new git item + parameterisation + SPN grant). Also: the Source tab renders no Database/Table/Use-query fields until 'Connection type' is set \u2014 a blank Connection type is why the table picker appears missing.",
    "WORKSPACE/GIT SYNC STATE 2026-07-31 after Erik's Save + Update: workspaceHead == remoteCommitHash == aa28744 (the orchestrator's commit of the local half). Workspace bronze-to-silver notebook verified via getDefinition \u2014 NOTE this is a 202 long-running operation for Notebooks (poll the Location header then GET {op}/result), unlike DataPipeline getDefinition which returns 200 inline. Content is byte-identical (ignoring EOL) to the committed repo version, and passes: defines correct_procurement_date, applies it at the bronze read (df1_all), filters the CORRECTED frame, old raw-date SQL pushdown gone, exactly one '# PARAMETERS CELL'. This closes the silent-corruption risk \u2014 running against a stale workspace notebook would have written still-transposed silver dates on a GREEN run.",
    "\u2705 FULL PIPELINE RUN GREEN 2026-07-31 \u2014 job b56a43b9-81d2-4776-b182-e10832359a8b, triggered via REST POST /v1/workspaces/{ws}/items/{pipelineId}/jobs/instances?jobType=Pipeline (202 + Location). status=Completed, failureReason=null, 12:24:43Z -> 12:43:13Z (~18.5 min). All 10 activities Succeeded on the FIRST attempt, no retries: bronzecopy_procurement_transactional 27s, bronzecopy_supplier_ref 25s, bronzecopy_EUSupplyShares 28s, bronzecopy_GlobalSupplyShares 25s, bronze_EPI 104s, bronze_WGI 119s, bronze-to-silver 141s, silver-to-gold 488s, data_quality_checks 263s, pipeline_error_handler 82s. No serverless auto-pause hit (the DB was warm from the connection test), so the retry=3/300s policy was not exercised \u2014 it remains the mitigation for a cold run. acceptanceCriteria[6] met: green end to end with zero RefreshDataflow activities.",
    "\u2705 CRITERION 4+5 VERIFIED ON LIVE DATA 2026-07-31 (not just unit tests). ROW COUNTS: bronze_procurement_transactional=132 and bronze_supplier_ref=11, EXACTLY matching the pre-migration baseline; Delta log confirms real writes (procurement commits advanced 47->49, supplier_ref 45->47, active files consolidated 2->1 by the overwrite). DATE CORRECTION: read the live bronze parquet (8b9cace5-159b-461d-a840-a3540af9eb64.parquet) via OneLake + pyarrow \u2014 132 rows, 0 nulls, Date is date32[day], min=2028-02-24, max=2031-12-24, distinct day-of-month = {'24'} ONLY, distinct years = {2028,2030,2031}. That is the RAW transposed shape, proving the Copy activity does not transform (as intended). Applying date(day+2000, month, year-2000): day 24 -> year 2024 for every row, and years 2028/2030/2031 -> days 28/30/31. silver_procurement reads min=2024-01-31 max=2024-12-31 over 132 rows \u2014 the predicted corrected range. DECISIVE PARITY EVIDENCE: the PRE-migration bronze (commit 47, written by the old dataflow which corrected during ingestion) carried that SAME 2024-01-31..2024-12-31 range, so the relocated notebook correction reproduces the retired dataflow's output exactly.",
    "METHOD NOTE for future Delta inspection: aggregate stats over ALL ACTIVE files (checkpoint + replay), never over 'the newest commit that has stats'. A first attempt did the latter and reported silver as min=max=2024-12-31 \u2014 a single file's range, not the table's, which looked like catastrophic date collapse. The corrected pass over active files gave the true 2024-01-31..2024-12-31. Also note the post-migration bronze add action carries NO date-column min/max in its stats (the pre-migration dataflow-written one did), so range checks on bronze must read the parquet rather than trust stats.",
    "RETIREMENT COMPLETED 2026-07-31 per rules/feature-retirement.md. Step 1 snapshot: 3 files, byte-identical to the live workspace definition. Step 2 commit pin: re-pinned from the snapshot-time 04ed718 to e9f594e183dec565764970fb2b4638b823023a07, the commit immediately BEFORE the removal commit 3197a9b, per the directional convention (`git log 04ed718..e9f594e -- fabric/bronze_azureSQLdb2table.Dataflow/` is empty, so both are equivalent restore sources). Step 3 archive placement: .claude/support/retired/bronze-azuresqldb2table-dataflow/ with manifest + spec-excerpt covering all FOUR spec locations. Step 4 spec annotation: NOT DONE \u2014 routes through /iterate per DEC-016, the single remaining deliverable. Step 5 discoverability: added to dashboard-state.json user_notes as a 'Retired Features' section; dashboard regenerated. WORKSPACE ITEM DELETED: DELETE /v1/workspaces/{ws}/items/47f7a4ab-... -> HTTP 200, re-verified GET -> 404; only EPI_file2table and WGI_file2table remain as Dataflow items. This was required separately because deploy-fabric.yml sets no orphan/unpublish option (confirmed with a positive control), so fabric-cicd would never have removed it.",
    "MANIFEST RATIONALE CORRECTED 2026-07-31: it repeated the false 'Fabric is a different tenant from the Azure subscription' claim as part of why Key Vault was rejected. Corrected in place \u2014 one tenant (a7d750d0), two az accounts, only the tenant-level fabricuser has Fabric access. The Copy-activity decision never depended on that claim and stands on its own (credential in Fabric's connection store, SPN-safe, survives publish).",
    "project_definition.md DELIBERATELY NOT UPDATED despite 5 references to the dataflow (lines 133/143/635-638/847/1039). Its first line reads '> **DEPRECATED:** Superseded by `.claude/spec_v1.md`. Kept for Git history.' It is a frozen historical artifact, so editing it would be wrong. This removes it from the remaining-work list rather than completing it. README.md and .claude/support/documents/architecture/orchestration.md WERE updated.",
    "orchestration.md REWRITE WAS WIDER THAN task-048 STRICTLY REQUIRED \u2014 flagged deliberately. The doc had drift from at least three earlier tasks: 4 bronze activities documented vs 6 live, bronze_WGI/bronze_EPI still called RefreshDataflows (task-035 made them notebooks), 'Retry: 0 attempts' on every activity (task-011 set real policies), data_quality_checks and pipeline_error_handler absent entirely (task-022/026), and a Stage 4 'Copy job1 (InvokeCopyJob)' Warehouse Sync that does not exist in the pipeline. Leaving those adjacent to the task-048 edit would have left known-false statements in a doc being touched for accuracy. Rewritten from the LIVE definition with runtimes measured from run b56a43b9.",
    "SPEC ANNOTATION APPLIED 2026-07-31 via /iterate (DEC-016 route, propose-approve-apply, user approved both rounds). Four '**Retired (2026-07-31)**' markers added WITHOUT excising section content: \u00a7 Data Architecture/Ingestion Method (~150), \u00a7 Orchestration Stage 1 table (~634), \u00a7 Current State Assessment (~835), \u00a7 Development Workflow/Naming Conventions/Dataflows (~998). Stage 1 table row 4 replaced by two Copy rows (EPI renumbered to 6) with the historical row preserved verbatim in the note beneath \u2014 the ONE deliberate departure from the non-excision convention, approved as N1, because that table describes the LIVE pipeline and a dead row in it misleads. Stage 2 dependency count corrected 5 -> 6. Frontmatter updated: 2026-07-31. Spec index regenerated and verified fresh.",
    "\u26a0\ufe0f PROCESS FINDING \u2014 MY OWN AUDIT WAS INCOMPLETE, CAUGHT ONLY BY POST-APPLY VERIFICATION. The /iterate proposal claimed FOUR spec locations, derived by grepping `bronze_azureSQLdb2table` and `bronze_procurement`. A post-apply sweep on the broader term 'dataflow' found THREE more stale claims: line 1536 (Technical Decisions #5 \u2014 the CANONICAL error-handling description \u2014 named a 'procurement RefreshDataflow' activity and listed RefreshDataflow among non-notebook activities), line 114 and line 1451 (both said connection strings are 'managed via Fabric dataflow connections'; they are now managed via Fabric connections used by Copy activities). All three fixed in an approved round 2. LESSON: when auditing spec coverage for a retirement, grep the FEATURE CLASS ('dataflow'), not just the item name \u2014 an item-name grep silently misses prose that describes the mechanism generically. This is the same class of error as the rules/agents.md positive-control rule, one level up: the probe worked, but the probe TERM was too narrow.",
    "KNOWN REMAINING DRIFT, DELIBERATELY NOT FIXED (out of task-048 scope, user-approved exclusion as N6): spec_v1.md line 1457 says EPI's source is 'file-based dataflow ingestion'. That is false \u2014 task-035 moved EPI to bronze_ingest_epi.Notebook, and line 210 of the same spec already states EPI_file2table.Dataflow is retired, so the spec contradicts itself. This is task-035 drift, not task-048's. Left for a future /iterate rather than silently widening this retirement's scope.",
    "VERIFY-AGENT ATTEMPT 1 = FAIL (2026-07-31), consistency_check only. All 7 criteria verified PASS, with criterion 2 recorded as pass_with_documented_deviation. The verifier re-derived rather than trusted: (a) `git show 04ed718:.../pipeline-content.json` confirms the pre-migration bronze_procurement RefreshDataflow typeProperties held ONLY dataflowId/workspaceId/notifyOption/dataflowType \u2014 no parameters \u2014 so the mashup's `Value.NativeQuery` look-back branch was UNREACHABLE DEAD CODE and bronze always full-loaded; the migration lost nothing real. (b) Delta TIME TRAVEL at pre-migration versions (bronze_procurement_transactional @ v47, bronze_supplier_ref @ v45) independently reproduced 132 and 11, matching current state \u2014 a stronger check than the orchestrator's forward-only baseline. (c) az ad group show resolved Fabric-SPN-Access to dbe8361c, matching the connection's Group role assignment, and the two HttpServer connections carry a byte-identical assignment shape. (d) The parity fixtures are NOT tautological \u2014 pre_migration_bronze_df is built from _mashup_corrected_date, an independent Python reimplementation, and compared against the notebook's own function.",
    "FIXES FOR VERIFY ATTEMPT 1 (2026-07-31). (1) MAJOR notebook comment: bronze-to-silver lines ~440-443 claimed the mashup's source-side p_from_date pushdown was 'inherited by the replacing Copy activity' and 'that contract is unchanged'. Both false \u2014 neither Copy activity has sqlReaderQuery, p_from_date, or any @pipeline expression \u2014 and it contradicted spec_v1.md:156 and orchestration.md written by this same task. Rewritten to state plainly that there is NO bronze-side filter, that the mashup's look-back sat behind an unreachable branch, and that the Copy activities reproduce actual pre-migration runtime behaviour. TRACEABLE TO THE ORCHESTRATOR: the false claim originated in a mid-flight SendMessage that described the mashup's internal filter ordering and told the agent to 'preserve that ordering when you reason about the parameter contract' \u2014 the agent over-extended it to the Copy activity. (2) MAJOR stale live-references: 13 fixed across 7 files (error_recovery_playbook.md, fabric_workspace.md, incremental_load_strategy.md x3, data_sources.md x2, medallion_architecture.md, run-bronze.md x2, fabric-artifacts-inventory.md x3). (3) MINOR spec_v1.md:701 'all 5' -> 'all 6' \u2014 the second site of an already-approved correction applied at only one site. (4) MINOR files_affected expanded 4 -> 17 to match reality.",
    "\u26a0\ufe0f ROOT CAUSE OF THE INCOMPLETE ENGINE-CONSUMER AUDIT \u2014 worth more than the fix. The original audit command ended in `| head -40`. It TRUNCATED, and the truncation silently hid every consumer under .claude/commands/, .claude/support/documents/, and docs/. The truncation was actually NOTICED once, when fabric/parameter.yml appeared to be missing from the results \u2014 but it was treated as a one-off, that single file was checked by hand, and THE AUDIT WAS NEVER RE-RUN. So the note asserting '10 consumers' was written from truncated output while its own author knew the output had been truncated. Two compounding lessons: (a) never pipe an audit/enumeration probe through `head` \u2014 cap output by narrowing the query, not by truncating results; (b) when a probe is found to have been lossy, RE-RUN IT WHOLE \u2014 patching the one observed gap leaves the unobserved ones, and the resulting count reads as authoritative. This sits alongside rules/agents.md 'Negative Findings Require a Positive Control': the positive control confirmed the probe WORKED, which gave false confidence while the RESULT SET was silently clipped.",
    "\u26a0\ufe0f\u26a0\ufe0f VERIFY ATTEMPT 2 = FAIL \u2014 SAME CHECK, SAME DEFECT CLASS, THIRD OCCURRENCE. Attempt 1's issues 1, 3, 4 confirmed resolved (verifier re-derived issue 1 rather than trusting: pipeline-content.json has 0 sqlReaderQuery / 0 RefreshDataflow / 0 dataflowId, both Copy activities use SqlServerSource with table selection only, and the snapshot mashup.pq line 9 + `git show 04ed718:` confirm the look-back branch was unreachable). Issue 2 only PARTIALLY resolved. THE ROOT CAUSE IS THE REMEDIATION METHOD, NOT THE MISSED FILES: the fix swept the verifier's ENUMERATED FILE LIST instead of re-running the discovery probe whole. An untruncated sweep WAS run \u2014 but only on the ITEM NAME (`bronze_azureSQLdb2table`, 57 hits, clean). The FEATURE-CLASS sweep (`RefreshDataflow`, `bronze_procurement` excluding `_transactional`) was never re-run, even though 'grep the feature class, not the item name' is the lesson this task had ALREADY recorded in note 73 after the /iterate round-2 miss, and 'when a probe is found lossy, re-run it whole' is note 90 written in the attempt-1 remediation itself. Writing the lesson down twice did not prevent the third recurrence; only the verifier caught it. STANDING RULE FOR THIS PROJECT: a verification issue that enumerates INSTANCES OF A CLASS must be remediated by re-running the DISCOVERY PROBE, never by closing the listed instances \u2014 the enumeration is a sample, not the population.",
    "ROUND-3 FIXES 2026-07-31, driven by RE-RUNNING THE DISCOVERY PROBES WHOLE rather than by any enumerated list. Three untruncated sweeps: (1) `RefreshDataflow` repo-wide, (2) `bronze_procurement` minus `_transactional`, (3) `dataflow` under docs/. 10 edits across 8 files: run-full-pipeline.md (the same falsehood set already rewritten out of orchestration.md \u2014 4 bronze activities not 6, all three as RefreshDataflow, missing DQ + error handler, phantom Stage-4 InvokeCopyJob Warehouse Sync); docs/setup/TROUBLESHOOTING.md (operational runbook pointing at the deleted item \u2014 rewritten around the four-way error taxonomy with auto-pause as the most likely cause and an explicit 'do NOT set retry 0'); pipeline_schedule.md; fabric_workspace.md activity count 8->10; error_recovery_playbook.md x3 (notifyOption route now obsolete, retry table, section heading); medallion_architecture.md; incremental_load_strategy.md; data-flow-diagram.md. ALSO fixed the spec ordinal collision my own approved renumber introduced: Stage 1 ends at row 6 while the Stage 2 list restarted at '6.' \u2014 renumbered 6/7/8 -> 7/8/9.",
    "DELIBERATELY OUT OF SCOPE after round-3 sweeps, each checked and excluded on purpose: decision-004 + error_handling_strategy.md + friction.jsonl (archival/point-in-time records; decision-004 is additionally DEC-016-gated \u2014 its activity arithmetic IS now wrong and the verifier raised it as a friction marker, but rewriting an archival decision record is not this task's call); run-silver.md:178, external_data_automation.md:608, bronze-to-silver notebook-content.py:624 (task-035 WGI/EPI drift); mermaid `B1[bronze_procurement]` nodes in README.md, data_coverage_flow.md, data_quality_architecture.md, data-flow-diagram.md (bronze TABLE-layer nodes, not activity claims); coding_standards.md:148, performance-baselines.md:38, performance_monitor.py:270, tests/test_retry_ordinal.py:51 (table names / synthetic fixture, already flagged in manifest restore_notes item 8); .playwright-mcp/ captured output; spec_v1.md:1457 (user-excluded task-035 drift).",
    "\ud83d\udd34 ESCALATED 2026-07-31 after 3 failed verification attempts, all on consistency_check ONLY, all on the same defect class. All 7 acceptanceCriteria passed in every attempt; the deliverable works (pipeline green, 132/11 matched, dates correct). THE ROOT CAUSE MIGRATED ONE AXIS PER ROUND: (r1) probe truncated by `| head -40`; (r2) remediation swept the verifier's enumerated file list instead of re-running discovery; (r3) discovery re-run whole but only on the ITEM NAME; (r4) term correctly widened to the FEATURE CLASS but the SEARCH ROOT narrowed \u2014 probe 3 was literally scoped `dataflow under docs/`, so `.claude/support/documents/**` and `.claude/commands/**` were never covered by any feature-class sweep in any round. Probes 1 and 2 (`RefreshDataflow` repo-wide; `bronze_procurement` minus `_transactional`) WERE run whole and verified clean. Both earlier lessons had been written into these notes BEFORE the round that violated them \u2014 prose capture demonstrably did not prevent recurrence.",
    "VERIFIER'S TEMPLATE-GAP PROPOSAL (worth promoting beyond this task): rules/agents.md \u00a7 'Negative Findings Require a Positive Control' governs whether a probe WORKS, and attempt 2 established that a class-enumeration finding must be closed by re-running discovery \u2014 but NOTHING requires a discovery probe to declare and justify its SEARCH ROOT. A probe restricted to a subtree is a truncation by another name and still reports as authoritative. Proposed rule: a discovery probe closing a class-enumeration finding must be repo-wide by default; any path restriction must be stated together with its exclusion rationale in the same note that reports the result. Candidate for /feedback -> template promotion.",
    "SCOPE TRANSFER 2026-07-31 \u2014 USER DECISION (option C). The cross-file documentation-consistency remainder that failed verification three times is MOVED OUT of task-048 into task-049. Rationale given at the decision point: (a) all 7 of task-048's acceptance criteria pass and its engineering is independently verified working; (b) the drift is NOT all task-048's \u2014 task-035's WGI/EPI residue is interleaved through the SAME files, and decision-004's activity arithmetic is stale too, so fixing one migration's slice repeatedly re-encounters the others; (c) a single dedicated pass closes the whole class. task-049 carries the full population, the probe-discipline criteria, and the known-instance sample. task-048's remaining scope is therefore its 7 acceptance criteria ONLY \u2014 all of which passed on every attempt. NOTE this is a scope narrowing recorded in both task files, not a quiet pass: the failures are preserved verbatim in verification_history attempts 1-3, and the defects are not dismissed, they are owned by task-049."
  ],
  "partial_completion": {
    "completed_subtargets": [
      "CorrectedDate relocation into fabric/bronze-to-silver.Notebook/notebook-content.py (acceptanceCriteria[3])",
      "src/transformations/procurement_dates.py mirror + tests/test_procurement_dates.py parity suite (reference-implementation contract)",
      "Retirement snapshot + manifest at .claude/support/retired/bronze-azuresqldb2table-dataflow/ (orchestrator-written; acceptanceCriteria[5] partial \u2014 spec annotation + discoverability still owed)",
      "fabric/parameter.yml rewritten (orchestrator, 2026-07-31): KNOWN LIMITATION block narrowed to EPI_file2table/WGI_file2table, corrected to say the Gen2 credential is bound INSIDE THE DATAFLOW EDITOR (both previous locations documented there were wrong), plus a new Copy-activity replacement-pattern section incl. the connection-sharing prerequisite and the REST roleAssignments call. ALSO fixed the unrelated-but-wrong 'connection GUID is in the URL' guidance at the top of the file, replacing it with the REST /v1/connections recipe. Validated: yaml.safe_load OK; fabric_cicd._parameter._utils.validate_parameter_file -> True. The bronze_azureSQLdb2table key_value_replace entry (line ~190) deliberately LEFT INTACT.",
      "pipeline-content.json Copy activities \u2014 built in the Fabric UI by Erik, audited live: 0 failures/0 warnings (see notes). RefreshDataflow removed; bronze-to-silver re-wired to 6 deps.",
      "Pipeline committed (b88aeab), pulled locally; parameter.yml re-pointed off the dead dataflowId onto the connection GUID under find_replace (validates True, 0 dangling JSONPaths); full run green; criteria 0,1,3,4,6 verified live.",
      "Dataflow removed from repo (commit 3197a9b) AND from the Fabric workspace (REST DELETE -> 200, GET -> 404).",
      "Retirement steps 1,2,3,5 complete; discoverability entry added to dashboard-state.json and dashboard regenerated.",
      "Docs: README.md + orchestration.md updated (orchestration.md rewritten from live state); project_definition.md correctly left alone as a DEPRECATED artifact.",
      "Spec annotation applied via /iterate \u2014 4 retirement markers + Stage 1 table rewrite + 3 additional stale claims corrected in round 2. Retirement steps 1-5 now ALL complete."
    ],
    "remaining_subtargets": [
      "verify-agent pass \u2014 task-048 cannot reach Finished without task_verification.result == 'pass'."
    ],
    "resume_instructions": "Implementation complete; all 7 acceptance criteria addressed. Awaiting independent verification.",
    "confidence": "high"
  },
  "verification_history": [
    {
      "attempt": 1,
      "timestamp": "2026-07-31T15:45:00Z",
      "result": "fail",
      "summary": "All 7 acceptanceCriteria PASS (criterion 2 pass_with_documented_deviation). Failed on consistency_check: 2 major stale-reference defects + 2 minor. Verifier independently re-derived the baseline via Delta time travel (v47/v45 = 132/11), confirmed the pre-migration RefreshDataflow passed no parameters (so the mashup look-back branch was dead code), and resolved Fabric-SPN-Access to dbe8361c matching the connection role assignment."
    },
    {
      "attempt": 2,
      "timestamp": "2026-07-31T17:05:00Z",
      "result": "fail",
      "summary": "Issues 1,3,4 resolved; issue 2 only PARTIALLY. All 7 criteria still hold (210+21 tests pass). Failed again on consistency_check: 3 more live-facing surfaces describing the deleted activity as current (run-full-pipeline.md, docs/setup/TROUBLESHOOTING.md, docs/guides/pipeline_schedule.md)."
    },
    {
      "attempt": 3,
      "timestamp": "2026-07-31T18:25:00Z",
      "result": "fail",
      "summary": "ESCALATED at threshold. All 7 criteria pass; consistency_check fails a 4th time. 7 stale statements across 5 files. Root cause migrated again: round 3 widened the probe TERM to the feature class but narrowed the SEARCH ROOT to docs/, so .claude/support/documents/** and .claude/commands/** were never feature-class swept in any round."
    }
  ],
  "task_verification": {
    "result": "fail",
    "timestamp": "2026-07-31T18:25:00Z",
    "attempt": 3,
    "escalated": true,
    "checks": {
      "files_exist": "pass",
      "consistency_check": "fail",
      "spec_alignment": "pass",
      "output_quality": "pass",
      "runtime_validation": "pass",
      "integration_ready": "pass",
      "scope_validation": "pass"
    }
  }
}
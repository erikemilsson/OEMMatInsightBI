{
  "id": "task-053",
  "title": "Investigate the data_quality_checks runtime regression (+43% vs baseline)",
  "description": "task-012_5's retest measured data_quality_checks at 242 s avg against a 169 s baseline, with all four post-optimization observations (292/217/217, plus 231 at the 14:08 preliminary) above the 206 s baseline maximum and no overlap between the ranges. It is the only per-activity finding in the retest that clears a non-overlap test, and it now accounts for 21% of pipeline runtime against 17% at baseline. Causation is NOT established: task-012_3 never modified this notebook, so attributing it to V-Order or the broadcast hints is unfounded. The leading hypothesis is that gold_quality_history accumulates rows on every pipeline run — it is referenced 26 times in data_quality_checks — so if any check scans it unbounded, runtime grows monotonically with the number of runs ever executed. That would make this a design characteristic surfacing as an apparent regression rather than damage from the optimization work, and it would get worse indefinitely once task-010 puts the pipeline on a schedule.",
  "status": "Blocked",
  "priority": "P2",
  "difficulty": 3,
  "owner": "both",
  "phase": "2",
  "phase_name": "Silver / Gold Transformations & Data Quality",
  "spec_section": "Data Quality & Validation",
  "spec_fingerprint": "sha256:6d611c630bbad0f511430446d96c0db6b1fa80dace66133af7f97966583666f5",
  "spec_version": "1",
  "section_fingerprint": "sha256:ab8feb951cd64aefdb8d8ecd1f5c7a4a64a7233e21ccd44a873422cc7735e0da",
  "section_snapshot_ref": "",
  "estimated_hours": 2,
  "files_affected": [
    "fabric/data_quality_checks.Notebook/notebook-content.py"
  ],
  "acceptanceCriteria": [
    "The gold_quality_history read pattern in data_quality_checks is characterised statically first — every read is identified and classified as bounded (filtered to a run or window) or unbounded (whole-table scan). This half needs no Fabric access and should be done before anything is run.",
    "The hypothesis is tested against data, not just inspection: gold_quality_history row count is recorded alongside the activity duration, so growth-vs-runtime can be correlated across the runs already measured in performance_optimized.md.",
    "The conclusion states plainly whether accumulated history explains the regression, and if it does not, says so and leaves the cause open rather than substituting a second guess.",
    "If unbounded scans are confirmed as the cause, the fix bounds the scan window rather than truncating history — the trend data is the point of the table (three quality-observability tables track quality over time per the data_quality_architecture doc).",
    "The finding is written back into performance_optimized.md's regression section, which currently records the hypothesis as untested."
  ],
  "dependencies": [
    "task-012_5"
  ],
  "decision_dependencies": [],
  "notes": [
    "CREATED 2026-08-03 (/work, Erik-approved) from the task-012_5 retest. This is the single actionable finding the retest produced — everything else was a null result or a dismissed transient.",
    "EVIDENCE: performance_optimized.md § What got worse (regressions) item 1, and the per-run tables for Runs 1-3. Baseline 120/180/206 s (the 206 is the one exact baseline figure; runs 1-2 were gap-derived), retest 292/217/217 s, preliminary 231 s.",
    "WHY owner=both AND why Claude can act first (so this is correctly Pending, not Blocked): AC1 is pure static analysis of a notebook already in the repo — no Fabric access needed. Only AC2's row count requires Erik to run a query. This is the opposite of the FB-100 shape, where a both-owned task with an empty files_affected cannot start until the human acts.",
    "DO NOT assume the optimization caused this. task-012_3 changed silver-to-gold (V-Order, broadcast hints, caching) and bronze-to-silver (caching); it did not touch data_quality_checks. A confident-sounding causal story here would be exactly the kind of unfounded attribution the retest write-up deliberately avoided.",
    "URGENCY LINK: task-010 (pipeline scheduling) makes this worse if the hypothesis holds — a daily scheduled run adds history every day, so an unbounded scan degrades without bound.",
    "[PHASE A COMPLETE 2026-08-03 via implement-agent] AC1 static analysis done: exactly ONE unbounded data read of gold_quality_history (validate_historical_trend / Check 12 trend_validation — spark.table filtered by metric_name only, distinct+orderBy+limit(2) bounds output not scan, O(total rows)); plus 2 metadata-only probes (tableExists/.columns) and 2 appends. The \"26 references\" were comment/print mentions, not data scans. Schema finding: gold_quality_history has no run_id, is unpartitioned, run id = refresh_timestamp (two writers stamp separately) -> any AC4 fix must filter on refresh_timestamp. AC2-prep: 5 queries for Erik + 3-test correlation framework (size refutation / per-check exec_ms / growth consistency). Framed as falsifiable hypothesis, NOT a conclusion; live alternative suspect = gold-fact scans task-012_3 modified, if refuted. Full analysis persisted to .claude/support/workspace/task-053_AC1_analysis.md. REMAINING (Erik-gated): AC2 = Erik runs the row-count queries; AC3-5 gated on that data. No files modified in fabric/.",
    "[VERIFIED 2026-08-03 attempt 1 — PASS (Phase A increment)] AC1 static analysis + AC2-prep independently confirmed. STATUS -> Blocked on Erik's AC2 data: run the row-count queries in .claude/support/workspace/task-053_AC1_analysis.md § AC2-prep (queries #1-#4, plus #5 if baseline date known) against the oem_lh SQL endpoint, then /work re-dispatches Claude for AC3 (conclude) / AC4 (fix if confirmed) / AC5 (write-back to performance_optimized.md). Blocked (not Finished) because ACs 2-5 are incomplete; not In Progress because it is Erik's turn, not Claude's.",
    "PROVENANCE REFRESH 2026-08-04 (/work, Erik-approved 'handle the follow-ups'): spec_fingerprint 0e1cb6c1 -> a85ec535. Section 'Data Quality & Validation' section_fingerprint ab8feb95 UNCHANGED (byte-identical across this span, so section-level not touched). Cosmetic refresh: stops Step 1b re-running granular drift analysis that narrows to nothing. Task remains Blocked on Erik's AC2 row-count queries (see .claude/support/workspace/task-053_AC1_analysis.md).",
    "PROVENANCE REFRESH 2026-08-04 (second hop, same session -- FR-051 spec edit): spec_fingerprint a85ec535 -> 6d611c63. FR-051 reconciliation edited ## Data Transformations (gold_dim_indicator source + parent_indicator attribution), changing the full-spec fingerprint; this task's own section (Data Quality & Validation) is byte-identical across that edit, so section_fingerprint remains current. Task status and ACs unchanged."
  ],
  "user_feedback": "",
  "created_date": "2026-08-03",
  "updated_date": "2026-08-04",
  "completion_date": "2026-08-03",
  "verification_attempts": 0,
  "verification_history": [
    {
      "attempt": 1,
      "result": "pass",
      "timestamp": "2026-08-03T23:30:00Z",
      "checks": {
        "files_exist": "pass",
        "consistency_check": "pass",
        "spec_alignment": "pass",
        "output_quality": "pass",
        "runtime_validation": "not_applicable",
        "integration_ready": "pass",
        "scope_validation": "pass"
      },
      "issues": [
        {
          "severity": "minor",
          "description": "AC1 reasoning characterizes the scan as 'O(total rows in table)'; the precise cost is O(total files scanned + dq_overall_score rows read), still monotonic in run count — noting for accuracy, not a defect."
        },
        {
          "severity": "minor",
          "description": "files_affected lists the notebook, but Phase A is analysis-only and the notebook was NOT modified — the listing is the analysis subject / eventual AC4 edit target, not a Phase A edit. Cosmetic metadata mismatch; left as-is (forward-looking)."
        }
      ],
      "notes": "Phase A increment (AC1 + AC2-prep) INDEPENDENTLY VERIFIED pass. AC1 confirmed: exactly ONE unbounded data read of gold_quality_history (validate_historical_trend / Check 12, lines 1430-1455; metric_name filter is a selector not a run/window predicate; .limit(2) bounds 2-period output NOT the scan -> TakeOrderedAndProject full scan + priority queue; df reused at 1452-1455 re-filters the unbounded df -> 3 passes over all dq_overall_score rows). 2 metadata probes correctly non-row-scans; 2 appends confirmed; '26 references' = comment/print mentions; load-bearing data-read count = 1. Schema verified: no PARTITIONED BY, no run_id, run id = refresh_timestamp (silver-to-gold2 lines 3486-3496). AC2-prep: 5 queries use only schema columns, runnable against oem_lh; 3-test framework sound; TEST 2 verified (exec_ms in all_check_results/stdout but NOT in QUALITY_HISTORY_COLUMNS -> not persisted). Discipline sound: falsifiable hypothesis, not a conclusion; defers to note 35 (task-012_3 did not touch data_quality_checks); alternative suspect (gold-fact scans) flagged as follow-up, not a conclusion. No fabric/ files modified; no fabricated row count. STATUS DECISION: set to Blocked (NOT Finished) — ACs 2(run)-5 are Erik-gated and incomplete; Finished would falsely claim completion. NOT In Progress — no active Claude work, it is Erik's turn. Blocked = waiting on Erik's AC2 data dependency. Deviation from pass->Finished protocol, justified by incomplete Erik-gated ACs and corroborated by verify-agent recommendation. user_review_pending=true (both-owned; Erik's action is the gate).",
      "scope": "Phase A increment (AC1 + AC2-prep). ACs 2-5 Erik-gated, not verified this round."
    }
  ],
  "task_verification": {
    "result": "pass",
    "timestamp": "2026-08-03T23:30:00Z",
    "attempt": 1,
    "scope": "Phase A increment (AC1 static analysis + AC2-prep). NOT full-task verification — ACs 2-5 are Erik-gated and incomplete.",
    "checks": {
      "files_exist": "pass",
      "consistency_check": "pass",
      "spec_alignment": "pass",
      "output_quality": "pass",
      "runtime_validation": "not_applicable",
      "integration_ready": "pass",
      "scope_validation": "pass"
    },
    "notes": "Phase A increment (AC1 + AC2-prep) INDEPENDENTLY VERIFIED pass. AC1 confirmed: exactly ONE unbounded data read of gold_quality_history (validate_historical_trend / Check 12, lines 1430-1455; metric_name filter is a selector not a run/window predicate; .limit(2) bounds 2-period output NOT the scan -> TakeOrderedAndProject full scan + priority queue; df reused at 1452-1455 re-filters the unbounded df -> 3 passes over all dq_overall_score rows). 2 metadata probes correctly non-row-scans; 2 appends confirmed; '26 references' = comment/print mentions; load-bearing data-read count = 1. Schema verified: no PARTITIONED BY, no run_id, run id = refresh_timestamp (silver-to-gold2 lines 3486-3496). AC2-prep: 5 queries use only schema columns, runnable against oem_lh; 3-test framework sound; TEST 2 verified (exec_ms in all_check_results/stdout but NOT in QUALITY_HISTORY_COLUMNS -> not persisted). Discipline sound: falsifiable hypothesis, not a conclusion; defers to note 35 (task-012_3 did not touch data_quality_checks); alternative suspect (gold-fact scans) flagged as follow-up, not a conclusion. No fabric/ files modified; no fabricated row count. STATUS DECISION: set to Blocked (NOT Finished) — ACs 2(run)-5 are Erik-gated and incomplete; Finished would falsely claim completion. NOT In Progress — no active Claude work, it is Erik's turn. Blocked = waiting on Erik's AC2 data dependency. Deviation from pass->Finished protocol, justified by incomplete Erik-gated ACs and corroborated by verify-agent recommendation. user_review_pending=true (both-owned; Erik's action is the gate).",
    "issues": [
      {
        "severity": "minor",
        "description": "AC1 reasoning characterizes the scan as 'O(total rows in table)'; the precise cost is O(total files scanned + dq_overall_score rows read), still monotonic in run count — noting for accuracy, not a defect."
      },
      {
        "severity": "minor",
        "description": "files_affected lists the notebook, but Phase A is analysis-only and the notebook was NOT modified — the listing is the analysis subject / eventual AC4 edit target, not a Phase A edit. Cosmetic metadata mismatch; left as-is (forward-looking)."
      }
    ],
    "evidence": []
  },
  "test_protocol": null,
  "user_review_pending": true,
  "recovery_state": "Blocked on Erik AC2 data: run row-count queries in .claude/support/workspace/task-053_AC1_analysis.md § AC2-prep",
  "out_of_spec": false,
  "absorbed_into": "",
  "interaction_hint": "dashboard"
}

# Sync Tasks (DEPRECATED)

> **This command has been replaced by `/status`.** Use `/status` for a read-only view of project state.
>
> This redirect will be removed in a future session.

When this command is invoked, inform the user:

```
The /sync-tasks command has been replaced by /status.

Usage:
  /status    → Quick read-only view of project state (tasks, progress, decisions)

For task management and state changes, use /work instead.
```

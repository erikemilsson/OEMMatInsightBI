# Testing Dependencies for OEMMatInsightBI
# Install with: pip install -r requirements-test.txt

# Core Testing Framework
pytest>=8.0.0

# PySpark — match the local verification runtime (pyspark 4.0.1 + JDK 17, see root
# CLAUDE.md "Gotchas"). CI sets up Java 17 too, so CI and local run the SAME major,
# keeping cast/ANSI semantics aligned (spark.sql.ansi.enabled=false everywhere).
pyspark>=4.0.0,<5.0.0

# HTML test reports — used by the CI `--html` step in .github/workflows/test.yml
pytest-html>=4.1.0

# NOTE: black / flake8 / pylint are installed separately in the code-quality job
# (test.yml), not from this file. Coverage, xdist, mock, timeout, sugar, and mypy
# plugins are intentionally absent — none are wired into the CI invocation.